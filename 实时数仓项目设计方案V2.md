# 实时数仓项目设计方案 V2（最终版）

> 生成日期：2026-08-24
> 决策：学生版物理四层电商实时数仓（最终决策已定，不再比较方案）
> 角色：电商实时数仓实施主负责人
> 依据：`离线项目事实包.md` + `实时项目实施接口交接包V2.md`（AI A 只读审计，binlog 运行时已核实）
> V1 → V2：按最终决策修正交易接入（Flink CDC）、作业结构（两作业→三作业）、DWS Upsert、口径可加性、slot 规划

---

## 0. 决策基线（最终决策 10 条）

| # | 决策 | 状态 |
|---|---|---|
| 1 | 学生版物理四层电商实时数仓 | ✅ 固定 |
| 2 | 交易数据：WSL2 本地 MySQL gmall_rt + **Flink CDC**（binlog） | ✅ 固定 |
| 3 | 行为日志：生成器持续回放 → WSL2 本地 Kafka | ✅ 固定 |
| 4 | 作业：先两作业跑通，稳定后拆三作业 | ✅ 固定 |
| 5 | 代码：新项目复制并改造生成器，离线仓库冻结 | ✅ 固定 |
| 6 | API：Flink SQL 为主，少量 DataStream | ✅ 固定 |
| 7 | 首期流量域/交易域独立，不做跨域漏斗 | ✅ 固定 |
| 8 | 不加 Doris/ClickHouse/Paimon/Redis | ✅ 固定 |
| 9 | 不用 VM Kafka / VM MySQL 作为实时运行环境 | ✅ 固定（交接包 I.1：维持；VM CDC 只读消费纳入二期可选） |
| 10 | 招聘实时项目不作为简历主体、不视为已完成成果 | ✅ 固定 |

**事实修正（相对事实包 K15）**：VM MySQL binlog 运行时实查已开启（`log_bin=1, binlog_format=ROW, binlog_row_image=FULL, 30天`）——旧表述"binlog 未开启"作废。WSL2 本地 MySQL 8.0 容器默认 ROW，实施时显式确认 `log_bin=ON, binlog_format=ROW, binlog_row_image=FULL`。

**新项目路径**：`C:\Users\31099\ecommerce-realtime-data-warehouse`（WSL2: `/mnt/c/Users/31099/ecommerce-realtime-data-warehouse`）。开工第一步确认路径不存在，存在则报告不覆盖。

---

## 1. V1 → V2 修正对照

| V1 设计 | V2 修正 | 原因 |
|---|---|---|
| 业务数据"生成器直投 Kafka 模拟 CDC" | **Flink CDC 直读 MySQL binlog**（生成器分阶段写入 MySQL） | 最终决策 2；WSL2 自有 MySQL 可控，binlog 可配置；不推断"离线无 binlog 配置 = 本地不能用 CDC" |
| 单作业并行度 2+1 / 2 slot | MVP 两作业并行度 1、TM ≥ 2 slot；三作业并行度 1、TM ≥ 3 slot | 最终决策 7；资源以容器实际启动 + Flink UI 验证为准 |
| DWS append 流 | **DWS Upsert Kafka**，输出当前绝对值（非 delta） | 决策 4；迟到/重放可覆盖 |
| 中间层 1 个 dwd 聚合 topic | DWD 按域拆分 4 topic | 决策 3；避免异构 topic 混淆 |
| 指标可加性未明确 | 固定口径 + 禁止 SUM(分钟)→日 | 决策 5；F.2 红线 |
| 对账基准日 08-24 | **冻结测试日 2026-09-01** + manifest | 交接包 G：08-24 含手动补数噪声，不可严格对账 |
| Job2 称"无状态 sink" | 视实现：小时/天累计、用户去重 = **有状态**；仅覆盖写入可称无状态 | 决策 6 |

---

## 2. 组件版本锁定与兼容依据

| 组件 | 版本 | 兼容依据 | 获取方式 |
|---|---|---|---|
| Flink | **1.19.1**（`flink:1.19.1` 镜像） | Flink CDC 3.1.x 官方支持 Flink 1.18/1.19/1.20 | docker.io |
| Flink CDC | **3.1.1**（`flink-sql-connector-mysql-cdc-3.1.1.jar`） | 支持 MySQL 8.0（ROW+FULL 已验证 VM 同版本 8.0.46）；Flink 1.19 兼容矩阵内 | Maven Central |
| Flink Kafka/Upsert Kafka | **flink-sql-connector-kafka-3.2.0-1.19.jar** | 与 Flink 1.19 对应版本 | Maven Central |
| Flink JDBC sink | **flink-connector-jdbc-3.2.0-1.19.jar + mysql-connector-j-8.4.0.jar** | JDBC 3.2.0-1.19 对应 Flink 1.19 | Maven Central |
| Kafka | **3.7.1**（`apache/kafka:3.7.1`，KRaft 单节点） | **实施修正**：apache/kafka 官方镜像自 3.7 起发布，3.5.2 无镜像（实测 pull 404）；3.7.1 KRaft 支持完整，Flink 1.19 Kafka connector 兼容 Kafka 3.x | docker.io |
| MySQL | **8.0.4x**（`mysql:8.0`） | 交接包 E.4：8.0 支持 `VALUES()` 语法（A2 无需改）；8.4 才需改造 → **选 8.0** | docker.io |
| Grafana | **11.1.x**（`grafana/grafana:11.1.0`） | 稳定版，MySQL 数据源 | docker.io |
| 运行环境 | Docker Desktop WSL2 backend；JDK 由 Flink 镜像自带 | — | — |

**版本组合风险（待实施验证）**：CDC 3.1.1 + Flink 1.19 + MySQL 8.0 为官方兼容组合；实测以"Flink SQL 建 CDC source 能 SELECT 到 payment 表"为验收（Day 3）。

---

## 3. 最终架构

### MVP 两作业（第 1-3 周主形态）

```
行为日志生成器 V2（持续回放）
  → Kafka ods_traffic_log
交易生命周期生成器 V2（分阶段写）
  → MySQL gmall_rt（9 表）→ binlog
                            │
        ┌───────────────────┴───────────────┐
        │ Job1（并行度 1）                    │
        │  ODS Sources → DWD Kafka          │
        │   · Kafka source: ods_traffic_log │
        │   · CDC source: 4 流水表 + 维表    │
        │   · schema 解析/校验/去重/规范化    │
        │   → dwd_traffic_event             │
        │   → dwd_order_detail              │
        │   → dwd_payment_detail            │
        │   → dwd_refund_detail             │
        └───────────────────────────────────┘
                          │
        ┌─────────────────┴─────────────────┐
        │ Job2（并行度 1）                    │
        │  DWD Kafka → DWS Upsert Kafka     │
        │    同时输出 ADS MySQL（同作业分支）  │
        │  · 窗口聚合（watermark+allowed    │
        │    lateness，迟到→独立输出）        │
        │  · 日 UV/日支付用户独立去重计算     │
        │  → dws_traffic_1m / dws_trade_1m  │
        │  → dws_traffic_day / dws_trade_day│
        │  → MySQL gmall_report_rt（幂等     │
        │    upsert，当前绝对值）             │
        └───────────────────────────────────┘
                          │
                     Grafana 看板
```

### 最终三作业（第 3 周资源稳定后拆分，完成后才可称"物理四层学生版"）

```
Job1：ODS Sources → DWD Kafka（同上）
Job2：DWD Kafka → DWS Upsert Kafka（dws_*_1m / dws_*_day）
Job3：DWS Upsert Kafka → ADS MySQL（gmall_report_rt，覆盖式 upsert）
```

**物理分层声明（简历红线）**：仅两作业运行时，是"逻辑四层、物理 3 段"学生版；**三作业完成前禁止称"物理四层"**。

---

## 4. Topic / MySQL 表 / 作业清单

### 4.1 Kafka Topic 清单

| Topic | 语义 | 格式 | 生产者 | 消费者 | 说明 |
|---|---|---|---|---|---|
| ods_traffic_log | ODS 行为日志 | JSON（V2 契约） | 生成器 V2 | Job1 | 原样入库，不落地 |
| dwd_traffic_event | DWD 流量明细 | JSON（清洗后） | Job1 | Job2 | 主键=event_id |
| dwd_order_detail | DWD 订单明细 | JSON/changelog | Job1 | Job2 | **首期无 ADS 消费方**（SKU TopN 二期），保留以维持"至少区分"结构，成本低（CDC 通道已建） |
| dwd_payment_detail | DWD 支付明细 | JSON/changelog | Job1 | Job2 | GMV 主数据 |
| dwd_refund_detail | DWD 退款明细 | JSON/changelog | Job1 | Job2 | 退款金额主数据 |
| dws_traffic_1m | DWS 流量分钟 | **Upsert** | Job2 | Job3/对账 | 主键=metric_date+window_start+metric_name+dimension_key |
| dws_trade_1m | DWS 交易分钟 | **Upsert** | Job2 | Job3/对账 | 同上 |
| dws_traffic_day | DWS 流量日 | **Upsert** | Job2 | Job3/对账 | 日 UV 独立去重计算 |
| dws_trade_day | DWS 交易日 | **Upsert** | Job2 | Job3/对账 | 日支付用户独立去重 |

> 删减说明：dwd_order_detail 保留但首期无 ADS 消费方——若第 2 周资源紧张，可先不产出该 topic（Job1 注释掉对应分支即可），二期随 SKU TopN 恢复；解释：GMV/退款/支付用户数均不依赖订单明细。

### 4.2 MySQL 清单

**gmall_rt（CDC 源库，9 表全订阅）**：user_info / base_province / base_category / spu_info / sku_info / order_info / order_detail / payment_info / order_refund_info
（DDL 复用 business_ddl.sql + init_data.sql，`USE gmall` → `USE gmall_rt`）

**gmall_report_rt（结果库，按首期 6 指标建表）**：

| 表 | 主键 | 写入 |
|---|---|---|
| ads_traffic_1m | (metric_date, window_start, metric_name, dimension_key) | Upsert |
| ads_trade_1m | 同上 | Upsert |
| ads_traffic_day | (metric_date, metric_name, dimension_key) | Upsert |
| ads_trade_day | 同上 | Upsert |

通用列：`metric_value DECIMAL(18,2) / BIGINT` + `updated_at`。确定性主键 + 覆盖式 upsert（`INSERT ... ON DUPLICATE KEY UPDATE`），**禁止重复执行会累加的 SQL**。

### 4.3 作业清单

| 作业 | 输入 | 输出 | API | 状态 | checkpoint | 并行度 | 故障影响 | 重放 |
|---|---|---|---|---|---|---|---|---|
| Job1 | Kafka ods_traffic_log + CDC 4 流水表 | DWD 4 topic | Flink SQL 为主 | 有（去重状态） | 30s | 1 | 全链路断 | 是 |
| Job2 | DWD 4 topic | DWS 4 topic + MySQL | Flink SQL 为主 | **有**（窗口/日去重/累计） | 30s | 1 | DWS/ADS 断 | 是 |
| Job3（最终） | DWS 4 topic | MySQL | Flink SQL | 无状态（仅覆盖写） | 30s | 1 | 仅 ADS 断 | 是 |

**Job2 状态声明（决策 6）**：MVP 阶段 Job2 承担窗口聚合、日 UV/日支付用户独立去重、天累计 → **有状态作业**。仅当拆出 Job3 且 Job3 只做"读 DWS 当前绝对值 + 同主键覆盖写 MySQL"时，Job3 才可称无状态 sink。

---

## 5. 数据契约

### 5.1 行为日志 V2（原字段全部保留，新增 4 字段）

```json
{"common": {"uid": 100515, "platform": "app", "province_id": 7, "ts": "2026-09-01 08:00:05"},
 "page": {"page_id": "home"},
 "action": {"action_id": "browse", "item": null},
 "ts": "2026-09-01 08:00:05",
 "event_id": "a3f2...",
 "event_time_ms": 1756713605000,
 "biz_date": "2026-09-01",
 "source_timezone": "Asia/Shanghai"}
```

| 新字段 | 语义 | 规则 |
|---|---|---|
| event_id | 稳定唯一 ID | `md5(f"{biz_date}\|{i}\|{uid}\|{ts}\|{action_id}\|{page_id}\|{item}")`，i=生成器文件内累积行号；**生成器需 `--offset` 续号**（append 模式防冲突）；同 seed 同参数同 offset → 同 event_id |
| event_time_ms | 内部事件时间 | **UTC epoch 毫秒**；字面回填模式（--ts）按 Asia/Shanghai 解释转 UTC；实时模式（无 --ts）按 UTC 直转；`source_timezone` 显式标注解析规则 |
| biz_date | 业务日期 | event_time_ms 转 Asia/Shanghai 后取日期 |
| source_timezone | 解析规则 | "Asia/Shanghai"（字面回填）或 "UTC"（实时） |

**禁止**：Flink 运行时自增值构造 event_id（多并行不唯一、与生成器不可对齐）。

### 5.2 交易生命周期 V2（分阶段写入 gmall_rt，选交接包 D.3-a 路）

| 阶段 | 操作 | 时间语义 | 行数预期 |
|---|---|---|---|
| 1 | INSERT UNPAID 订单 + 明细（order_info 全量先以 UNPAID 落库） | order_time | 200 单 / ≈400 明细 |
| 2 | UPDATE 订单→PAID（order_status, update_time=pay_time）+ INSERT payment（SUCCESS） | pay_time = order_time + 1~60min | ≈176 支付 |
| 3 | 少量订单→REFUNDED + INSERT refund（SUCCESS） | refund_time = order_time + 3~48h | ≈14 退款 |

**关键约束（D.3-a）**：单次运行内分阶段输出（同 rng 序列不变），靠 update_time 模拟时间流逝 → **保"同 seed 与离线历史基准对账"承诺**。三条路已定 a，不改。
**CDC 视角**：依赖 binlog 变更事件本身（INSERT/UPDATE 真实执行即产生事件），不依赖 update_time 字段。
**幂等**：全表 ON DUPLICATE KEY UPDATE（8.0 VALUES() 兼容，A2 语法不动）。

### 5.3 口径锚点（固定 6 口径，源自交接包 F.1）

| # | 指标 | 定义 | 对账基准 |
|---|---|---|---|
| 1 | PV | browse 事件数 | 批 SQL：COUNT(*) browse |
| 2 | 分钟 UV | 分钟内 browse 用户去重 | 不参与日对账（见红线） |
| 3 | 日 UV | 自然日内 browse 用户重新去重（**独立计算，不 SUM 分钟**） | 批 SQL：COUNT(DISTINCT uid) |
| 4 | GMV | SUCCESS 支付金额，按支付时间（create_time），**不减退款** | check_source_data.sql 第 4 节 |
| 5 | 支付订单数 | DISTINCT order_id | 批 SQL |
| 6 | 日支付用户数 | 自然日 DISTINCT user_id 且 payment_amount>0（**独立去重**） | 批 SQL |
| 7 | 退款金额 | SUCCESS 退款金额，按退款时间 | check_source_data.sql 第 5 节 |

**禁止**：`SUM(分钟UV)` → 日 UV；`SUM(分钟支付用户数)` → 日支付用户数；browse+click 作为对账 PV。
**口径红线**：日志用户池（1000/活跃 500）与业务用户池（100）仅 ~10% 重叠，**不可跨域关联**（首期两域独立，不做跨域漏斗）。

---

## 6. Docker Compose 资源规划

```yaml
services:
  mysql:
    image: mysql:8.0
    environment: { MYSQL_ROOT_PASSWORD: ..., MYSQL_DATABASE: gmall_rt }
    command: ["mysqld",
      "--log-bin=mysql-bin", "--binlog-format=ROW", "--binlog-row-image=FULL",
      "--server-id=1", "--binlog-expire-logs-seconds=2592000"]
    mem_limit: 1G
    ports: ["3306:3306"]
  kafka:
    image: apache/kafka:3.5.2   # KRaft 单节点
    mem_limit: 1.5G
    ports: ["9092:9092"]
  jobmanager:
    image: flink:1.19.1
    command: jobmanager
    mem_limit: 0.75G
    ports: ["8081:8081"]
    volumes: ["./flink-lib:/opt/flink/lib"]   # CDC/Kafka/JDBC/MySQL jar
  taskmanager:
    image: flink:1.19.1
    command: taskmanager
    mem_limit: 2G
    environment: { TASK_MANAGER_NUMBER_OF_TASK_SLOTS: "2" }   # MVP 2 slot；三作业阶段改 3
  grafana:
    image: grafana/grafana:11.1.0
    mem_limit: 0.5G
    ports: ["3000:3000"]
```

**预算合计 ≈ 5.75G**；WSL2 需 `.wslconfig` 限 `memory=8GB`（Windows 物理 16G）。**资源数字以容器实际启动 + Flink UI（8081）TM 注册 + docker stats 为准，不以纸面为准**。
**flink-lib 目录**：flink-sql-connector-mysql-cdc-3.1.1.jar、flink-sql-connector-kafka-3.2.0-1.19.jar、flink-connector-jdbc-3.2.0-1.19.jar、mysql-connector-j-8.4.0.jar。
**flink-conf**：state.backend=rocksdb（状态去重/日去重量大时），checkpoint.interval=30s。

---

## 7. Day 1-4 逐日任务与验收命令

| 天 | 任务 | 验收命令/标准（硬性） |
|---|---|---|
| **Day 1** | ①确认路径不存在，创建项目 + git init + 复制 A1/A2/A3/A4/A5 + 改造 USE 库名 ②写 Docker Compose（先起 MySQL+Kafka）③本机验证端口/网络 | `docker compose up -d mysql kafka`；`docker compose ps` 两服务 healthy；`mysql -h127.0.0.1 -e "SELECT 1"`；`kafka-topics.sh --bootstrap-server localhost:9092 --list` 可见 `__consumer_offsets`；`kafka-console-producer/consumer` 产消一条消息通过 |
| **Day 2** | ①起 Flink JM+TM（挂 flink-lib）②生成器 V2 行为日志持续回放 → Kafka ods_traffic_log ③Flink SQL client 消费打印 | Flink UI 8081 可见 1 TM 注册 ≥2 slot；`kafka-console-consumer --topic ods_traffic_log` 见 V2 JSON（含 event_id/event_time_ms/biz_date）；Flink SQL `SELECT COUNT(*)` 从 kafka source 读到 >0 |
| **Day 3** | ①MySQL 建 gmall_rt 9 表 + init_data.sql ②业务生成器 V2 分阶段灌数 ③验证 binlog ④Flink SQL 建 CDC source 测试 | `SHOW VARIABLES LIKE 'log_bin'`=ON、`binlog_format`=ROW、`binlog_row_image`=FULL；`SELECT COUNT(*) gmall_rt.order_info`=200、payment_info≈176、refund≈14、order_detail≈400；Flink SQL `SELECT COUNT(*)` 从 mysql-cdc source 读到 payment 表 >0 |
| **Day 4** | 跑通最小指标链路：Job1（ods_traffic_log→dwd_traffic_event）→ Job2（窗口聚合→MySQL）→ Grafana | **止损线**：`mysql gmall_report_rt -e "SELECT COUNT(*) FROM ads_traffic_1m"` > 0，且 PV 值正确（≥1 分钟窗口有数据）；Grafana 面板出数。**若 MySQL 仍无指标：暂停物理分层，先修通单作业链路** |

---

## 8. 阻塞开工的问题（≤5）

结论先行：**可以开工**——无设计级阻塞。开工第一动作即验证以下 3 项环境前提（任一失败需先解决）：

1. **制品获取网络**：docker.io 拉镜像（flink/kafka/mysql/grafana）与 Maven Central 拉 4 个 jar 是否可达（github.com 已证实被墙；docker hub / maven 可能需换源或走代理 127.0.0.1:7897）。**阻塞级：高**——拿不到 jar 则 CDC 无法启用，需备选源。
2. **Docker Desktop + WSL2 状态未验证**：是否已安装、版本、`.wslconfig` 内存限制未配置（默认会吃满 WSL2 内存拖垮 Windows 16G）。**阻塞级：中**。
3. **本机端口冲突**：3306/9092/8081/3000 是否被本机服务占用（如已有 MySQL/开发服务）。**阻塞级：低**（可换映射端口）。
4. **生成器 V2 三项决策待用户拍板**（本方案已给建议，确认即不阻塞）：event_id 公式 + `--offset`（交接包 I.2）、阶段化选 a 路（I.4）、MySQL 8.0（I.5）。
5. **Flink CDC 3.1.1 + MySQL 8.0 容器 binlog 默认配置实测**：理论兼容组合，Day 3 验收项（CDC SELECT 通），非开工阻塞。

---

## 9. 开工后第一项具体操作（等"开始实施"）

```
① docker version + docker compose version（Docker Desktop 状态）
② curl 验证制品源可达性（docker.io / Maven Central 或代理）
③ netstat 检查 3306/9092/8081/3000 占用
④ 三项全过 → 创建 C:\Users\31099\ecommerce-realtime-data-warehouse + git init
⑤ 复制 A1/A2/A3/A4/A5，改 USE gmall→gmall_rt
```

---

## 10. 对交接包 V2 第 I 节 8 个问题的确认

| # | 问题 | 本方案确认 |
|---|---|---|
| I.1 | 不接 VM CDC 是否维持 | ✅ 维持；VM CDC 只读消费纳入二期可选（不改任何 VM 配置） |
| I.2 | event_id = md5(biz_date\|i\|uid\|ts\|action_id\|page_id\|item)，i 文件累积行号，需 --offset | ✅ 接受，纳入 V2 生成器改造清单 |
| I.3 | event_time_ms 双模式解析 + source_timezone 显式标注 | ✅ 确认 |
| I.4 | 阶段化三条路 | ✅ 选 **a**（单 run 分事务段，保同 seed 对账） |
| I.5 | MySQL 版本 | ✅ 8.0.x（VALUES() 兼容，A2 语法不动） |
| I.6 | CDC 初始快照窗口 | ✅ 首期串行：灌数完成 → 再启 CDC 作业（初始快照=当前全量，快照后接增量）；启动顺序写进启动脚本 |
| I.7 | 分钟 UV/支付用户去重语义 | ✅ 分钟=窗口内去重（append 分钟值）；日=自然日独立重新去重（**不 SUM 分钟**）；分钟之和≠日值写入对账差异说明（预期差异） |
| I.8 | 测试日 2026-09-01 | ✅ 接受为严格对账日（seed=20260901，orders=200，count=2000，active-users=500，--ts="2026-09-01 08:00:00"），生成后记录 manifest（日期/seed/事件数/订单/支付/退款/checksum） |

---

## 11. 对账设计（第 3 周）

- 对账日：**2026-09-01**（冻结），离线不生成该日数据 → 单一来源
- 6 项：browse PV、日 UV、GMV、支付订单数、日支付用户数、退款金额
- 方法：实时结果（DWS/ADS） vs 独立批 SQL（按 check_source_data.sql 第 4/5 节语义重写，同一份 MySQL gmall_rt 数据）
- 时机：**watermark 和 allowed lateness 结束后执行**；极迟数据若无补偿，对账报告标记"已知差异"，不声称完全一致
- 粒度：`(biz_date, metric_name, 可选维度, batch_value, stream_value, absolute_diff, relative_diff, difference_reason)`
- 通过线：差异率 < 0.5% 或 difference_reason 明确（迟到/口径/去重语义）

---

## 12. 简历纪律（第 3-4 周对照执行）

完成哪项写哪项，附运行截图/测试记录/可复现命令。**当前禁止写**：Flink CDC / 物理四层 / 端到端 exactly-once / 实时离线对账 / Lambda 架构 / 高并发 / 生产级 / 高可用。
checkpoint 只证明状态恢复语义，**不自动证明 MySQL 端到端 exactly-once**（MySQL 幂等靠确定性主键 + 覆盖式 upsert 保证）。

---

*V2 最终版。等待用户回复"开始实施"后执行第 9 节第一项操作。*
