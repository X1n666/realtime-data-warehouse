# 项目开发日志（可重跑节点式）

> 用途：记录每一步做了什么、为什么这么做、如何验证。**任何节点出错，从该节点重跑即可**（生成器确定性 + 幂等 upsert 保证可复现）。
> 测试日：2026-09-01（--seed 20260901）。对账基准见 manifest_20260901.txt。
> 日志规则：节点间串行；验证不通过不得进入下一节点。

---

## 环境基线（2026-08-24，反复确认过的状态）

| 项 | 状态 |
|---|---|
| 项目根目录 | `C:\Users\31099\Desktop\实时数据分析平台`（中文目录名，compose 已用 `name:` 字段固定项目名） |
| Docker 5 容器 | mysql(healthy)/kafka(healthy)/jm/tm/grafana，compose 项目名 `ecommerce-realtime-data-warehouse`（网络名同） |
| MySQL | gmall_rt 9 表（100/200/398/182/12）+ gmall_report_rt 4 表 |
| Kafka topics | ods_traffic_log / dwd_traffic_event / dwd_payment_detail / dws_traffic_1m / dws_trade_1m |
| 行为日志 | 2000 条（seed 20260901，--active-users 500，08:00:00 起） |
| Flink | 3 作业 RUNNING（job1×1 + job2×2），ads_traffic_1m 33 窗口 PV=636 |

**环境事件记录**：
- Docker Desktop 两次意外退出（第 1 次用户操作目录时；第 2 次未知原因）→ 第 2 次导致 Kafka 容器层数据丢失（mysql/kafka 数据未挂 named volume）。
- **教训**：mysql/kafka 需要持久化卷 + restart 策略（待修复项，见"待办"）。
- 恢复方式：compose up -d → 重灌（确定性生成器可复现）→ 重提交作业。

---

## 节点 1：Job1 加 CDC 分支（payment_info → dwd_payment_detail）✅ 2026-08-24

### 做了什么
在 `flink-sql/job1_ods_to_dwd.sql` 追加三段：CDC source（payment_cdc）+ upsert-kafka sink（dwd_payment_detail）+ INSERT 透传。

### 为什么这么做（面试可讲的设计决策）
1. **source 声明 `PRIMARY KEY (id) NOT ENFORCED`**：mysql-cdc 输出 changelog（+I/-U/+U/-D），主键声明是下游按 key 合并 -U/+U 的前提。**不声明 = -U/+U 被当两条 append 记录 = 后续 GMV 聚合翻倍**。
2. **不做 ROW_NUMBER 去重**：id 唯一性由 MySQL PRIMARY KEY 保证（NOT ENFORCED 只是 Flink 不校验，上游已保证）。与流量域对照：Kafka 无主键 → 必须 event_id 去重。
3. **sink 主键选 id（支付流水）不选 order_id**：同订单可有多条支付流水，order_id 做主键会压丢流水、GMV 少算。
4. **sink 9 列 vs source 12 列**：丢 out_trade_no（与 trade_no 语义重叠）/ subject（冗余展示信息）/ update_time（CDC 位点由 Flink 管理）；保留 trade_no（渠道对账）、payment_type（维度）、callback_time（支付时长分析）。
5. **类型**：datetime → TIMESTAMP(0)（source，秒精度原生）→ sink TIMESTAMP(3) 需显式 CAST（Flink 不同精度不隐式转换）。

### 如何验证
- Flink UI：job1 变成 **2 个作业实例**（流量分支 + CDC 交易分支）均 RUNNING
- 消费 dwd_payment_detail topic：**182 条** = payment_info 182 行 ✓

### 出错重跑方法
```
# Kafka 数据丢（如 Docker 重启）：
docker compose up -d
# 重建 topic + 重灌日志（脚本见节点 1 命令历史）：
python data-generator/generate_action_log.py --seed 20260901 --count 2000 --active-users 500 --ts "2026-09-01 08:00:00" --output - | docker exec -i gmall_kafka /opt/kafka/bin/kafka-console-producer.sh --bootstrap-server localhost:9092 --topic ods_traffic_log
# 重提交作业：
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
```
> 幂等性：dwd_payment_detail 是 upsert-kafka（key=id），重放覆盖写不重复；流量域 ROW_NUMBER 去重后重放无副作用。

---

## 修复：job2 双 INSERT 共享 consumer group 导致 DWS 层空转（Day4 遗留 bug）✅

### 现象与根因
job2_dwd_to_dws.sql 里 dws/ads 两个 INSERT 提交为**两个独立作业**，但都引用同一个 source 表（group.id=job2_dwd_group）。Kafka 1 partition 的消息只会被 group 内一个 consumer 消费 → 一个作业拿全量，另一个空转。**Day4 只验证了 ads（636 ✓）漏了 dws**；实测 dws_traffic_1m 为 0。

### 修复
给 ads INSERT 定义独立 source `dwd_traffic_event_ads`（同 topic，group.id=job2_ads_group）→ 两作业各自消费全量。

### 验证
- dws_traffic_1m：33 窗口 PV=636 ✓（修复前 0）
- ads_traffic_1m：33 窗口 PV=636 ✓（不受影响）

### 面试价值（问题 #7）
多个 Flink SQL 作业复用同一 DDL source = 共享 consumer group = 消息被瓜分。**教训：每个作业分支必须独立 group.id；验证必须覆盖每一条输出路径**（Day4 只查了 ads 漏了 dws）。

---

## 节点 2：交易 DWS 日聚合（GMV/支付订单数/支付用户数）→ ads_trade_day ✅ 2026-08-24

### 做了什么
1. 新建 `flink-sql/job2_trade_dws.sql`：消费 dwd_payment_detail → 3 指标按支付日聚合 → dws_trade_day（upsert-kafka）+ ads_trade_day（MySQL JDBC）
2. **环境改造（slot 8 + 持久化）**：6 作业 > 4 slots（NoResourceAvailable 无限重启）→ TM slots 4→8；mysql/kafka 数据挂 named volume（kafka 镜像 log.dirs=/tmp/kraft-combined-logs）+ 全部容器 restart: unless-stopped（Docker 两次退出丢数据教训）→ recreate 后导入 mysqldump 备份 + 重灌日志 + 重提交作业

### 为什么这么做
1. **GMV 聚合选"非窗口 GROUP BY CAST(create_time AS DATE)"而非 TUMBLE 窗口**：非窗口 = 持续累计（upsert 更新当日值），无 watermark/迟到/窗口触发问题，支付记录任何时刻到达都归入其支付日——对账简单可靠；窗口聚合要等窗口结束才输出，测试日一次性回放尾部数据会被卡住（流量域 5 条尾部数据的教训）。
2. **交易日指标表无 window_start**（对照实际 DDL：日表 PK=(metric_date, metric_name, dimension_key)；分钟表才有 window_start）。
3. **3 指标用 UNION ALL 合并成 2 个 INSERT**（dws 1 + ads 1），避免 6 个作业；每个 INSERT 独立聚合状态（SUM / COUNT DISTINCT 各自计算）。
4. **金额 SUM 输出 DECIMAL(18,2)**：SUM(decimal(16,2)) 精度 = decimal(38,2) → 输出统一 CAST DECIMAL(18,2) 兼容通用表结构。
5. **GMV 口径**：WHERE payment_status='SUCCESS' 不过滤其他状态（锚点 4：SUCCESS 金额不退 refund——退款的扣减在 refund_amount 指标单独表达，不加到 GMV）。

### 如何验证（批流对账 3/6 指标）
| 指标 | 流值（ads_trade_day） | 批值（payment_info） | 结果 |
|---|---|---|---|
| gmv | 1,136,485.35 | 1,136,485.35 | ✓ |
| payment_order_count | 182 | 182 | ✓ |
| payment_user_count | 83 | 83 | ✓ |

### 出错重跑方法
```
# 重提交（作业丢失/失败时）：
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
# 环境重建（数据已持久化，无需重灌 MySQL；Kafka 若丢需重灌日志）：
docker compose up -d
```

---

## 节点 3：退款域（refund_detail CDC → refund_amount 指标）✅ 2026-09-03

### 做了什么
1. [job1_ods_to_dwd.sql](../flink-sql/job1_ods_to_dwd.sql) 追加第 3 分支：`refund_cdc` source（mysql-cdc 12 列全字段，server-id `5410-5414`，table-name=order_refund_info）+ `dwd_refund_detail` sink（upsert-kafka，7 列，PK id）+ 透传 INSERT（create_time CAST TIMESTAMP(3)）
2. 建 topic `dwd_refund_detail`（1 partition，与其他 topic 同规格）
3. [job2_trade_dws.sql](../flink-sql/job2_trade_dws.sql)：dws/ads 两个 INSERT 的 UNION ALL 各追加第 4 个 SELECT（`refund_amount` 指标，dws 版 FROM dwd_refund_detail、ads 版 FROM dwd_refund_detail_ads——独立 group.id 原则不破）
4. 重部署：cancel 旧版 job1×2 + job2_trade_dws×2 → 重提交新版。**cancel 后重提交 = 作业无状态全量重算 + upsert 覆盖写 = 幂等**（重算结果与旧值相同则覆盖写同值，下游数据不变）

### 为什么这么做（面试可讲）
1. **退款是独立指标体系，不是 GMV 的减项**：GMV 口径锚点 4 = SUCCESS 支付金额不退 refund；退款金额单独成指标（锚点 7），两表分算才能分别回答"卖了多少钱"与"退了多少"。
2. **退款源表与支付源表分开 CDC**：order_refund_info 有自己的流水 id/状态/时间，拆开才能按退款时间归日（退款可滞后支付 1-2 天，见下方验证——真实业务正是如此）。
3. **同一作业多 CDC source 必须独立 server-id 段**：refund 用 `5410-5414`，payment 用 `5400-5404`；同 MySQL broker 上相同 server-id 会被互相踢下线。
4. **sink 7 列丢 5 列**：refund_type/refund_num/refund_reason_*/update_time 属分析维度与 CDC 内部字段，明细层只保留聚合必需的 id/用户/订单/sku/金额/状态/时间。
5. **指标并入既有 INSERT 不加作业数**：4 指标 × 2 分支 = 2 作业；加第 5 个指标仍不加作业（UNION ALL 的每段独立聚合状态）。

### 如何验证（批流对账 4/6 指标）
先查批基准：refund SUCCESS 共 12 行、总额 49,064.19，且**跨 9/1–9/3**（生成器模拟退款滞后支付）→ 按日逐行核对流值：

| metric_date | 流值 refund_amount | 批值（order_refund_info） | 结果 |
|---|---|---|---|
| 2026-09-01 | 3,680.40 | 3,680.40（1 行） | ✓ |
| 2026-09-02 | 27,535.37 | 27,535.37（5 行） | ✓ |
| 2026-09-03 | 17,848.42 | 17,848.42（6 行） | ✓ |
| 合计 | 49,064.19 | 49,064.19 | ✓ |

- dwd_refund_detail topic = 12 条 ✓（= order_refund_info 12 行）
- GMV 仍 1,136,485.35 / 订单 182 / 用户 83 不变 ✓（退款不冲 GMV，指标间无串扰）
- Flink：job1 三分支 + job2_trade_dws 两作业（4 指标）RUNNING

### 出错重跑方法
```
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
```
（cancel 旧实例后提交；upsert 幂等，重放无副作用）

---

## 节点 4：流量日指标独立重算（browse_pv / day_uv → ads_traffic_day）✅ 2026-09-03

### 做了什么
1. **环境升级（slots 8→12）**：作业将达 9+ 个（job1×3 + 流量分钟 dws/ads + 日 dws/ads + 交易 dws/ads），8 slots 不够 → [docker-compose.yml](../docker-compose.yml)：TM slots 12、process 2048m、mem_limit 2.5g、`managed.fraction: 0.1`（流作业状态小，0.4 默认值浪费）。TM 重建期间作业从 checkpoint 自动恢复，无需重提。
2. [job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql) 追加：2 个最小列 source（`dwd_traffic_event_day` / `_day_ads`，独立 group.id，只声明 uid/action_id/biz_date/event_id）+ `dws_traffic_day`（upsert-kafka）+ `ads_traffic_day`（JDBC）+ 2 INSERT（UNION ALL：browse_pv + day_uv，非窗口 `GROUP BY CAST(biz_date AS DATE)`，browse 过滤同分钟口径）。
3. 建 topic `dws_traffic_day`；cancel 旧 job2_dwd_to_dws×2 → 重提交（4 分支）。
4. **批基准从生成器重放获得**：流量源在 Kafka（无 MySQL 批表可查）→ `generate_action_log.py --seed 20260901` 确定性重放 + python 统计 browse 计数/distinct uid（锚点批 SQL 的等价实现）。

### 为什么这么做（面试点）
1. **红线：日 UV 禁止 SUM 分钟 UV**（同一用户可跨多个分钟窗口活跃，分钟 UV 不可加）→ 日 UV 必须从明细 `COUNT(DISTINCT uid)` 独立重新去重（锚点 3 原话"独立重新去重"）。
2. **日 PV 也独立重算，不 SUM 分钟**：实测 SUM 分钟 = **636 ≠ 明细 641**——2000 条一次性回放尾部 5 条 browse 落在未触发窗口（窗口等 watermark 才触发）→ 日指标从明细兜底 = 窗口延迟不丢数。差异 5 条是**预期差异**，写入对账差异表（README I.7），不是 bug。
3. 聚合方式延续交易日模式：非窗口 GROUP BY = 持续累计 + upsert 覆盖（dws topic 实测 1→641 递增覆盖），无 watermark/迟到/尾部卡窗口问题。
4. **调试 bug（面试可讲）**：`Column 'metric_date' not found`——**UNION ALL 输出列名继承第一段**，第一段无 `AS` 别名时列名退化为 EXPR$N，外层 SELECT 引用失败。修复：只给第一段加别名（后续段继承）。当日 ads 分支首段漏别名（dws 分支首段有所以成功）。
5. 修复后**只提交第 4 段**（前 3 分支已在跑）：多 INSERT 文件重跑会重复提交所有分支 → 同 topic 同 group.id 重复消费 → 瓜分 bug（见"修复"节）。教训：多分支 SQL 文件改单个分支时，要么 cancel 全部重提（幂等），要么抽单段单独提。

### 如何验证（批流对账 5/6 指标）
批基准（生成器确定性重放 seed 20260901）：`2026-09-01 browse_pv=641, day_uv=365`

| 指标 | 流值（ads_traffic_day） | 批值 | 结果 |
|---|---|---|---|
| browse_pv | 641 | 641 | ✓ |
| day_uv | 365 | 365 | ✓ |

- dws_traffic_day topic：upsert 持续累计覆盖 ✓
- 分钟层不受影响：ads_traffic_1m SUM=636 ✓（= 641 − 5 尾部，预期差异）
- Flink：job1×3 + job2_dwd×4 + job2_trade×2 = 9 作业全部 RUNNING

### 出错重跑方法
```
# 全量重提（cancel 同名旧作业后）：
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
# 只换单个分支：抽该段（DDL+INSERT）成临时文件单独提交（见本节"为什么"5）
```

---

## 节点 5：批流对账（6 指标总表）✅ 2026-09-03

### 做了什么
汇总 6 指标做一次总对账：流侧查 gmall_report_rt（ads_traffic_day + ads_trade_day），批侧查 gmall_rt（payment_info / order_refund_info）+ 生成器确定性重放（流量源在 Kafka，无批表）。测试日 2026-09-01（seed 20260901，manifest_20260901.txt）。

### 对账总表（流 = 批 全 ✓）

| # | 指标 | 流值 | 批值（批 SQL/重放） | 结果 |
|---|---|---|---|---|
| 1 | browse_pv（日） | 641 | 641 | ✓ |
| 3 | day_uv | 365 | 365 | ✓ |
| 4 | gmv | 1,136,485.35 | 1,136,485.35 | ✓ |
| 5 | payment_order_count | 182 | 182 | ✓ |
| 6 | payment_user_count | 83 | 83 | ✓ |
| — | refund_amount（按日） | 9/1: 3,680.40 / 9/2: 27,535.37 / 9/3: 17,848.42 | 同 | ✓ |

（refund_amount 跨 9/1–9/3：生成器模拟退款滞后支付 1-2 天，聚合口径=按退款时间归日，流批逐日一致。）

### 差异说明（预期差异，非 bug——README I.7 定义）
1. **分钟 PV（636）≠ 日 browse_pv（641）**：差 5 条尾部 browse。根因：2000 条一次性回放，最后一个事件窗口未等 watermark 触发就无数据了 → 分钟窗口少 5；日指标从明细重算不受影响。**验证了"日指标独立重算"的必要性**（若日 PV 从分钟 SUM 上卷会丢 5）。
2. **分钟 UV 不参与日对账**：当前分钟表只产出 browse_pv；若产出分钟 UV 也不可 SUM 当日 UV（红线，同用户跨窗口）。

### 面试价值
- 每个指标都有**独立可复现的批基准**（MySQL 批 SQL + 生成器同 seed 重放），流侧 30s checkpoint 后任意时刻重查一致 → 论证流批一致性的方法本身可演示。
- 差异是**分类记录**的：真 bug（共享 group、重复计数）→ 修复并回归；预期差异（尾部窗口、分钟 vs 日不可加）→ 写清原因不消除。面试讲"你怎么知道流算对了"的答案在这里。

### 出错重跑方法
全部指标重跑 = 节点 2/3/4 的 SQL 重新提交（各自文件），批基准按 manifest + 重放命令重现；核对完成后重跑本表查询即可。

---

## 节点 6：拆三作业（Job3 无状态 sink）——物理四层成立 ✅ 2026-09-03

### 做了什么
1. [job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql) 精简：删 ads_traffic_1m/ads_traffic_day 两个直出 MySQL 的分支与专属 source → 只留 dws 计算（2 分支）
2. [job2_trade_dws.sql](../flink-sql/job2_trade_dws.sql) 精简：删 ads_trade_day 分支 → 只留 dws_trade_day（1 分支）
3. 新建 [job3_ads_sink.sql](../flink-sql/job3_ads_sink.sql)：3 分支无状态转发（dws_traffic_1m→ads_traffic_1m / dws_traffic_day→ads_traffic_day / dws_trade_day→ads_trade_day），每分支独立 group.id
4. 部署顺序（关键）：先 cancel 旧 job2 全 6 作业 → 提交 Job3（从 dws topic 存量接管写 MySQL，无空洞）→ 再提交精简 Job2（重算 dws topics，Job3 实时转发覆盖收敛）

### 为什么这么做（面试点，README 红线"拆作业前禁止写物理四层"）
1. **物理四层成立**：ODS(MySQL/Kafka) → DWD(CDC/去重) → DWS(聚合) → ADS(落库) 各一层专属作业，拓扑即简历上写的"四层实时数仓"。
2. **Job3 无状态论证（设计 V2 决策 6）**：读 DWS 聚合后的当前绝对值（upsert-kafka 同 key 已合并、无 changelog 计算）+ 同主键覆盖写 MySQL → 无窗口/去重/累计算子、无状态。好处：重启秒级、天然幂等（重放 N 次收敛同值）、可用多并行扩展吞吐。
3. **指标只算一次**：MVP 的 ads 直出分支从 DWD 明细重复计算（每个指标双份窗口/去重状态）；拆后 Job2 算一次写 dws topic，Job3 只转发 → 计算成本减半、口径天然一致（ads 与 dws 不再可能因两套计算差异打架）。
4. 三个 INSERT = 三个独立作业，group.id 必须独立（既有教训）。

### 如何验证
- 9 作业全部 RUNNING：job1×3（ODS→DWD）+ job2×3（DWD→DWS）+ job3×3（DWS→ADS）
- MySQL 与拆前逐值一致（拆作业不改数据是正确性验证）：
  - ads_traffic_1m：33 窗口、PV 汇总 636 ✓
  - ads_traffic_day：browse_pv 641 / day_uv 365 ✓
  - ads_trade_day：gmv 1,136,485.35 / 订单 182 / 用户 83 / refund 按日 3 行 ✓
- Job3 单作业 task 图应无聚合算子（纯 source→sink）

### 出错重跑方法
```
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
bash scripts/submit_sql.sh flink-sql/job3_ads_sink.sql job3_ads_sink
```
（cancel 同名旧作业后按 Job1→Job2→Job3 顺序提交）

---

## 节点 7：Grafana 看板（provisioning 自动注册）✅ 2026-09-03

### 做了什么
1. 新建 [grafana/provisioning/](../grafana/provisioning/)（datasources/mysql.yaml：MySQL 数据源 uid=mysql-rt + dashboards/dashboards.yaml：file provider 10s 热更新）
2. 新建 [grafana/dashboards/realtime-overview.json](../grafana/dashboards/realtime-overview.json)：8 面板——GMV/支付订单/支付用户/退款金额/日 PV/日 UV 六个 stat + 分钟 PV 柱状时间序列 + ADS 明细表
3. [docker-compose.yml](../docker-compose.yml) grafana 挂载两目录（只读）+ depends_on mysql healthy
4. `compose up -d grafana` 重建 → API 验证数据源/看板注册 → 代理查询面板 SQL 确认能取数

### 为什么这么做（面试点）
1. **provisioning 而非 UI 手点**：数据源/看板是配置文件 → 随 compose 可复现（环境重建不丢配置），简历讲"Grafana 面板代码化管理"。
2. **uid 固定（mysql-rt / rt-realtime-overview）**：dashboard JSON 与数据源解耦引用，不依赖 Grafana 自生成 uuid。
3. **时区处理**：ADS 的 window_start 是 MySQL DATETIME（本地墙上时间，无时区语义），Grafana MySQL 插件按 UTC 解析 → 时间序列面板用 `CONVERT_TZ(window_start,'+08:00','+00:00')` 显式归一到 UTC 再让 Grafana 显示 Asia/Shanghai（否则曲线偏移 8 小时）。
4. **stat 面板取"最新日值"**（ORDER BY metric_date DESC LIMIT 1）：指标表按日 upsert，看板永远展示当前值；分钟序列用 $__timeFilter 走 Grafana 时间范围。

### 如何验证
- `GET /api/datasources` → mysql-rt（MySQL 实时数仓 → mysql:3306）✓
- `GET /api/search` → rt-realtime-overview（电商实时数仓 · 实时指标总览）✓
- `POST /api/ds/query` 代理查 GMV → 1136485.35、日 UV → 365 ✓（面板 SQL 数据链路通）
- 浏览器 http://localhost:3000（admin/admin）可见看板

### 出错重跑方法
```
docker compose up -d grafana   # provisioning 文件改动后重建（或等 10s 热更新看板）
```

---

---

## 节点 8：分钟指标补齐（流量 UV + 交易分钟）+ 两个排障 ✅ 2026-09-03

### 做了什么
1. **流量分钟 UV**（[job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql)）：分钟 INSERT 改 UNION ALL 加 `browse_uv` 段（窗口内 COUNT DISTINCT uid）→ 验证 33 窗口：browse_uv 汇总 627 ≤ browse_pv 636 ✓（UV≤PV 语义成立）
2. **交易分钟表**：job2_trade_dws.sql 增加 `dws_trade_1m` sink + 分钟窗口 INSERT（gmv / payment_order_count 两指标 UNION ALL），source `dwd_payment_detail` 补 WATERMARK FOR create_time（-5s）；[job3_ads_sink.sql](../flink-sql/job3_ads_sink.sql) 加第 4 分支（dws_trade_1m→ads_trade_1m，JDBC 落 MySQL）
3. **排障 A —— 共享 consumer group**：日 INSERT 与分钟 INSERT 两个独立作业共用同一 source DDL（group job2_trade_dws_group）→ 1 partition 被 rebalance 瓜分，分钟作业消费中断在 12:04（43 窗口）。修复：分钟段独立 source DDL `dwd_payment_detail_1m` + 独立 group.id
4. **排障 B —— 事件时间乱序（真根因）**：换独立 group 后仍只 43 窗口 → 分层排查（见下）→ 发现 Kafka topic 内事件时间非单调 → 新建 [replay_dwd_payment_sorted.sh](../scripts/replay_dwd_payment_sorted.sh) 一次性按 create_time 排序重放 → job2_trade source 指向 `dwd_payment_detail_sorted`
5. **一键启动脚本** [start_env.sh](../scripts/start_env.sh)：compose up + 健康等待 + topic 数据层检查 + 按依赖顺序补齐作业（幂等可重跑）

### 为什么这么做（面试点）
1. **分钟粒度是趋势图基础**：日指标只有 1 个点画不了曲线；分钟 PV/UV/GMV 驱动 Grafana 时间序列（真实数仓的"实时"叙事 = 分钟级刷新）。
2. **排障 A 教训**：Flink SQL 文件内多个 INSERT = 多个独立作业，**每个作业消费的 source 都要独立 DDL + 独立 group.id**（job2_dwd 的日段早已独立成 dwd_traffic_event_day；trade 加分钟 INSERT 时漏了 → 同一坑踩第二次，证明"多作业共享 group"不是一次性失误而是结构性陷阱）。
3. **排障 B 教训（最值钱）**：分层排查法——先证数据透传（临时无窗口作业按小时 COUNT 全量 182 ✓，12:00+ 有 133 笔）排除 source/normalize → 再证窗口层（只 fire 43）→ dump topic 时间序列找乱序。机制：**upsert-kafka 的 watermark 按物理消费序推进，Kafka 内事件乱序 → 先读到晚事件水位跳高 → 其后到达的早窗口事件被判"迟到"丢弃**（watermark 只前进不后退）。
4. **乱序根因是"重灌伪影"不是系统缺陷**：测试数据 payment 的 id = 流水号，与支付时间错位；CDC snapshot 按主键扫描输出 → 非时间序。真实生产 Kafka 事件序 ≈ binlog 提交序 ≈ 发生序，不会有此问题。重排工具把数据重放成"真实事件流" = 与流量域 generator replay 同哲学（数据重放 = 确定性回放测试数据）。

### 如何验证（对账闭环）
- ads_trade_1m：**162 窗口**（08:21–23:27 全部有支付的分钟）× 2 指标
  - gmv 合计 1,119,295.10 = 日 GMV 1,136,485.35 − 尾部 23:32:16 单笔 17,190.25 ✓（分毫不差）
  - 订单 181 = 182 − 1 ✓；抽查 12:04/14:19/22:55 三个分钟窗口与 MySQL payment_info 同分钟聚合**逐值相等** ✓
- 尾部差异说明：[23:32,23:33) 单笔窗口不 fire（最后水位 23:32:11 < 窗口 end）——与流量尾部同机制，属预期（面试口径："测试日最后一条记录所在窗口因无后续事件推进水位而不触发"）
- 11 作业 RUNNING：job1×3 + job2_dwd×2 + job2_trade×2 + job3×4；日表 4 指标复查不变（gmv 1,136,485.35 / 182 / 83 / refund 49,064.19）

### 出错重跑方法
```
# 交易分钟重算（幂等：全新 group 从 earliest 全量重算，upsert 覆盖收敛）
bash scripts/replay_dwd_payment_sorted.sh     # 重排数据（可重跑，幂等：删 topic 重建再灌）
# cancel 旧 job2_trade_dws 后：
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws

# 日常重启环境（幂等）：
bash scripts/start_env.sh
```

---

---

## 节点 9：Grafana 看板分钟层补齐（9 面板）✅ 2026-09-03

### 做了什么
1. [realtime-overview.json](../grafana/dashboards/realtime-overview.json) 升级 8 → 9 面板：
   - 分钟 PV 面板（id 7）升级为**双系列**：browse_pv + browse_uv（分钟 UV 上同一时间轴对比，半宽）
   - 新增**分钟成交额 GMV 趋势面板**（id 9，timeseries，12 列宽，人民币单位）：ads_trade_1m WHERE metric_name='gmv'
2. 排障：Grafana admin 密码失效（`GF_SECURITY_ADMIN_PASSWORD` 只在 admin 用户**不存在**时生效，用户已存在则不变）→ 容器无数据卷 + 全 provisioning 管理 → `docker compose up -d --force-recreate grafana` 强重建 = 干净初始态，看板/数据源零丢失自动恢复（provisioning 可复现性实证）

### 为什么这么做（面试点）
1. **节点 8 的分钟指标要"看得见"**：分钟 UV/GMV 是趋势图数据源，看板闭环（数据→指标→可视化）是简历 demo 的最后一公里——打开 Grafana 即演示分钟级刷新。
2. **PV/UV 同图**：直观展示 UV≤PV 语义（面试可指图讲 watermark 窗口与去重差异）；GMV 用折线 + 金额单位区分交易域（与流量柱状视觉分层）。
3. **provisioning 全声明式**（数据源 uid + 看板文件）：容器删了重建自动恢复 —— 简历可讲"配置即代码，Grafana 零手工配置"。

### 如何验证
- API 读看板：9 面板注册 ✓（id 7 targets=2、id 9 GMV 面板在）
- `POST /api/ds/query` 代理执行两条新 SQL：GMV 分钟序列返回 **162 行**（= ads_trade_1m 窗口数）✓；browse_uv 返回 **33 行**（= 流量窗口数）✓
- 浏览器 localhost:3000（admin/admin）：GMV 曲线 08:21-23:27 分钟点、PV/UV 双曲线

### 出错重跑方法
```
docker compose up -d --force-recreate grafana   # 凭据/状态异常时强重建（全 provisioning，无手工配置）
# 面板文件改动：等 10s 热更新即可（dashboard provider），无需重建
```

---

## 待办（工程可靠性，简历可讲）
- [x] mysql/kafka 数据卷持久化（named volume），Docker 重启不丢数据（节点 2 已完成：mysql-data + kafka-data）
- [x] docker-compose 加 `restart: unless-stopped`（节点 2 已完成）
- [x] 环境一键启动脚本 `scripts/start_env.sh`（compose up + topic 检查 + 作业补齐，节点 8 已完成）
- [x] 分钟指标补齐：流量分钟 UV + 交易分钟 dws_trade_1m/ads_trade_1m（节点 8 已完成）
- [x] Grafana 看板分钟层补齐（节点 9：PV/UV 双系列 + 分钟 GMV 面板）
- [ ] 可选加分：dwd_order_detail 订单明细 CDC（接入订单域第三张 DWD 表——注意：需配套下游指标，避免"无消费者死表"）
- [ ] 可选加分：《Flink 学习与思考题复习笔记》与本项目交叉引用（学以致用对照）
