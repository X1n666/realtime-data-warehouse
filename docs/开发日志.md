# 项目开发日志（可重跑节点式）

> 用途：记录每一步做了什么、为什么这么做、如何验证。**任何节点出错，从该节点重跑即可**（生成器确定性 + 幂等 upsert 保证可复现）。
> 测试日：2026-09-01（--seed 20260901）。对账基准见 manifest_20260901.txt。
> 日志规则：节点间串行；验证不通过不得进入下一节点。

---

## 环境基线（2026-08-24，反复确认过的状态）

| 项 | 状态 |
|---|---|
| 项目根目录 | `C:\Users\31099\Desktop\实时数据分析平台`（中文目录名，compose 已用 `name:` 字段固定项目名） |
| Docker 5 容器 | mysql(healthy)/kafka(healthy)/jm/tm/grafana，compose 项目名 `ecommerce-realtime-data-warehouse`（网络名同） |
| MySQL | gmall_rt 9 表（100/200/398/182/12）+ gmall_report_rt 4 表 |
| Kafka topics | ods_traffic_log / dwd_traffic_event / dwd_payment_detail / dws_traffic_1m / dws_trade_1m |
| 行为日志 | 2000 条（seed 20260901，--active-users 500，08:00:00 起） |
| Flink | 3 作业 RUNNING（job1×1 + job2×2），ads_traffic_1m 33 窗口 PV=636 |

**环境事件记录**：
- Docker Desktop 两次意外退出（第 1 次用户操作目录时；第 2 次未知原因）→ 第 2 次导致 Kafka 容器层数据丢失（mysql/kafka 数据未挂 named volume）。
- **教训**：mysql/kafka 需要持久化卷 + restart 策略（待修复项，见"待办"）。
- 恢复方式：compose up -d → 重灌（确定性生成器可复现）→ 重提交作业。

---

## 节点 1：Job1 加 CDC 分支（payment_info → dwd_payment_detail）✅ 2026-08-24

### 做了什么
在 `flink-sql/job1_ods_to_dwd.sql` 追加三段：CDC source（payment_cdc）+ upsert-kafka sink（dwd_payment_detail）+ INSERT 透传。

### 为什么这么做（面试可讲的设计决策）
1. **source 声明 `PRIMARY KEY (id) NOT ENFORCED`**：mysql-cdc 输出 changelog（+I/-U/+U/-D），主键声明是下游按 key 合并 -U/+U 的前提。**不声明 = -U/+U 被当两条 append 记录 = 后续 GMV 聚合翻倍**。
2. **不做 ROW_NUMBER 去重**：id 唯一性由 MySQL PRIMARY KEY 保证（NOT ENFORCED 只是 Flink 不校验，上游已保证）。与流量域对照：Kafka 无主键 → 必须 event_id 去重。
3. **sink 主键选 id（支付流水）不选 order_id**：同订单可有多条支付流水，order_id 做主键会压丢流水、GMV 少算。
4. **sink 9 列 vs source 12 列**：丢 out_trade_no（与 trade_no 语义重叠）/ subject（冗余展示信息）/ update_time（CDC 位点由 Flink 管理）；保留 trade_no（渠道对账）、payment_type（维度）、callback_time（支付时长分析）。
5. **类型**：datetime → TIMESTAMP(0)（source，秒精度原生）→ sink TIMESTAMP(3) 需显式 CAST（Flink 不同精度不隐式转换）。

### 如何验证
- Flink UI：job1 变成 **2 个作业实例**（流量分支 + CDC 交易分支）均 RUNNING
- 消费 dwd_payment_detail topic：**182 条** = payment_info 182 行 ✓

### 出错重跑方法
```
# Kafka 数据丢（如 Docker 重启）：
docker compose up -d
# 重建 topic + 重灌日志（脚本见节点 1 命令历史）：
python data-generator/generate_action_log.py --seed 20260901 --count 2000 --active-users 500 --ts "2026-09-01 08:00:00" --output - | docker exec -i gmall_kafka /opt/kafka/bin/kafka-console-producer.sh --bootstrap-server localhost:9092 --topic ods_traffic_log
# 重提交作业：
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
```
> 幂等性：dwd_payment_detail 是 upsert-kafka（key=id），重放覆盖写不重复；流量域 ROW_NUMBER 去重后重放无副作用。

---

## 修复：job2 双 INSERT 共享 consumer group 导致 DWS 层空转（Day4 遗留 bug）✅

### 现象与根因
job2_dwd_to_dws.sql 里 dws/ads 两个 INSERT 提交为**两个独立作业**，但都引用同一个 source 表（group.id=job2_dwd_group）。Kafka 1 partition 的消息只会被 group 内一个 consumer 消费 → 一个作业拿全量，另一个空转。**Day4 只验证了 ads（636 ✓）漏了 dws**；实测 dws_traffic_1m 为 0。

### 修复
给 ads INSERT 定义独立 source `dwd_traffic_event_ads`（同 topic，group.id=job2_ads_group）→ 两作业各自消费全量。

### 验证
- dws_traffic_1m：33 窗口 PV=636 ✓（修复前 0）
- ads_traffic_1m：33 窗口 PV=636 ✓（不受影响）

### 面试价值（问题 #7）
多个 Flink SQL 作业复用同一 DDL source = 共享 consumer group = 消息被瓜分。**教训：每个作业分支必须独立 group.id；验证必须覆盖每一条输出路径**（Day4 只查了 ads 漏了 dws）。

---

## 节点 2：交易 DWS 日聚合（GMV/支付订单数/支付用户数）→ ads_trade_day ✅ 2026-08-24

### 做了什么
1. 新建 `flink-sql/job2_trade_dws.sql`：消费 dwd_payment_detail → 3 指标按支付日聚合 → dws_trade_day（upsert-kafka）+ ads_trade_day（MySQL JDBC）
2. **环境改造（slot 8 + 持久化）**：6 作业 > 4 slots（NoResourceAvailable 无限重启）→ TM slots 4→8；mysql/kafka 数据挂 named volume（kafka 镜像 log.dirs=/tmp/kraft-combined-logs）+ 全部容器 restart: unless-stopped（Docker 两次退出丢数据教训）→ recreate 后导入 mysqldump 备份 + 重灌日志 + 重提交作业

### 为什么这么做
1. **GMV 聚合选"非窗口 GROUP BY CAST(create_time AS DATE)"而非 TUMBLE 窗口**：非窗口 = 持续累计（upsert 更新当日值），无 watermark/迟到/窗口触发问题，支付记录任何时刻到达都归入其支付日——对账简单可靠；窗口聚合要等窗口结束才输出，测试日一次性回放尾部数据会被卡住（流量域 5 条尾部数据的教训）。
2. **交易日指标表无 window_start**（对照实际 DDL：日表 PK=(metric_date, metric_name, dimension_key)；分钟表才有 window_start）。
3. **3 指标用 UNION ALL 合并成 2 个 INSERT**（dws 1 + ads 1），避免 6 个作业；每个 INSERT 独立聚合状态（SUM / COUNT DISTINCT 各自计算）。
4. **金额 SUM 输出 DECIMAL(18,2)**：SUM(decimal(16,2)) 精度 = decimal(38,2) → 输出统一 CAST DECIMAL(18,2) 兼容通用表结构。
5. **GMV 口径**：WHERE payment_status='SUCCESS' 不过滤其他状态（锚点 4：SUCCESS 金额不退 refund——退款的扣减在 refund_amount 指标单独表达，不加到 GMV）。

### 如何验证（批流对账 3/6 指标）
| 指标 | 流值（ads_trade_day） | 批值（payment_info） | 结果 |
|---|---|---|---|
| gmv | 1,136,485.35 | 1,136,485.35 | ✓ |
| payment_order_count | 182 | 182 | ✓ |
| payment_user_count | 83 | 83 | ✓ |

### 出错重跑方法
```
# 重提交（作业丢失/失败时）：
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
# 环境重建（数据已持久化，无需重灌 MySQL；Kafka 若丢需重灌日志）：
docker compose up -d
```

---

## 节点 3：退款域（refund_detail CDC → refund_amount 指标）✅ 2026-09-03

### 做了什么
1. [job1_ods_to_dwd.sql](../flink-sql/job1_ods_to_dwd.sql) 追加第 3 分支：`refund_cdc` source（mysql-cdc 12 列全字段，server-id `5410-5414`，table-name=order_refund_info）+ `dwd_refund_detail` sink（upsert-kafka，7 列，PK id）+ 透传 INSERT（create_time CAST TIMESTAMP(3)）
2. 建 topic `dwd_refund_detail`（1 partition，与其他 topic 同规格）
3. [job2_trade_dws.sql](../flink-sql/job2_trade_dws.sql)：dws/ads 两个 INSERT 的 UNION ALL 各追加第 4 个 SELECT（`refund_amount` 指标，dws 版 FROM dwd_refund_detail、ads 版 FROM dwd_refund_detail_ads——独立 group.id 原则不破）
4. 重部署：cancel 旧版 job1×2 + job2_trade_dws×2 → 重提交新版。**cancel 后重提交 = 作业无状态全量重算 + upsert 覆盖写 = 幂等**（重算结果与旧值相同则覆盖写同值，下游数据不变）

### 为什么这么做（面试可讲）
1. **退款是独立指标体系，不是 GMV 的减项**：GMV 口径锚点 4 = SUCCESS 支付金额不退 refund；退款金额单独成指标（锚点 7），两表分算才能分别回答"卖了多少钱"与"退了多少"。
2. **退款源表与支付源表分开 CDC**：order_refund_info 有自己的流水 id/状态/时间，拆开才能按退款时间归日（退款可滞后支付 1-2 天，见下方验证——真实业务正是如此）。
3. **同一作业多 CDC source 必须独立 server-id 段**：refund 用 `5410-5414`，payment 用 `5400-5404`；同 MySQL broker 上相同 server-id 会被互相踢下线。
4. **sink 7 列丢 5 列**：refund_type/refund_num/refund_reason_*/update_time 属分析维度与 CDC 内部字段，明细层只保留聚合必需的 id/用户/订单/sku/金额/状态/时间。
5. **指标并入既有 INSERT 不加作业数**：4 指标 × 2 分支 = 2 作业；加第 5 个指标仍不加作业（UNION ALL 的每段独立聚合状态）。

### 如何验证（批流对账 4/6 指标）
先查批基准：refund SUCCESS 共 12 行、总额 49,064.19，且**跨 9/1–9/3**（生成器模拟退款滞后支付）→ 按日逐行核对流值：

| metric_date | 流值 refund_amount | 批值（order_refund_info） | 结果 |
|---|---|---|---|
| 2026-09-01 | 3,680.40 | 3,680.40（1 行） | ✓ |
| 2026-09-02 | 27,535.37 | 27,535.37（5 行） | ✓ |
| 2026-09-03 | 17,848.42 | 17,848.42（6 行） | ✓ |
| 合计 | 49,064.19 | 49,064.19 | ✓ |

- dwd_refund_detail topic = 12 条 ✓（= order_refund_info 12 行）
- GMV 仍 1,136,485.35 / 订单 182 / 用户 83 不变 ✓（退款不冲 GMV，指标间无串扰）
- Flink：job1 三分支 + job2_trade_dws 两作业（4 指标）RUNNING

### 出错重跑方法
```
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
```
（cancel 旧实例后提交；upsert 幂等，重放无副作用）

---

## 节点 4：流量日指标独立重算（browse_pv / day_uv → ads_traffic_day）✅ 2026-09-03

### 做了什么
1. **环境升级（slots 8→12）**：作业将达 9+ 个（job1×3 + 流量分钟 dws/ads + 日 dws/ads + 交易 dws/ads），8 slots 不够 → [docker-compose.yml](../docker-compose.yml)：TM slots 12、process 2048m、mem_limit 2.5g、`managed.fraction: 0.1`（流作业状态小，0.4 默认值浪费）。TM 重建期间作业从 checkpoint 自动恢复，无需重提。
2. [job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql) 追加：2 个最小列 source（`dwd_traffic_event_day` / `_day_ads`，独立 group.id，只声明 uid/action_id/biz_date/event_id）+ `dws_traffic_day`（upsert-kafka）+ `ads_traffic_day`（JDBC）+ 2 INSERT（UNION ALL：browse_pv + day_uv，非窗口 `GROUP BY CAST(biz_date AS DATE)`，browse 过滤同分钟口径）。
3. 建 topic `dws_traffic_day`；cancel 旧 job2_dwd_to_dws×2 → 重提交（4 分支）。
4. **批基准从生成器重放获得**：流量源在 Kafka（无 MySQL 批表可查）→ `generate_action_log.py --seed 20260901` 确定性重放 + python 统计 browse 计数/distinct uid（锚点批 SQL 的等价实现）。

### 为什么这么做（面试点）
1. **红线：日 UV 禁止 SUM 分钟 UV**（同一用户可跨多个分钟窗口活跃，分钟 UV 不可加）→ 日 UV 必须从明细 `COUNT(DISTINCT uid)` 独立重新去重（锚点 3 原话"独立重新去重"）。
2. **日 PV 也独立重算，不 SUM 分钟**：实测 SUM 分钟 = **636 ≠ 明细 641**——2000 条一次性回放尾部 5 条 browse 落在未触发窗口（窗口等 watermark 才触发）→ 日指标从明细兜底 = 窗口延迟不丢数。差异 5 条是**预期差异**，写入对账差异表（README I.7），不是 bug。
3. 聚合方式延续交易日模式：非窗口 GROUP BY = 持续累计 + upsert 覆盖（dws topic 实测 1→641 递增覆盖），无 watermark/迟到/尾部卡窗口问题。
4. **调试 bug（面试可讲）**：`Column 'metric_date' not found`——**UNION ALL 输出列名继承第一段**，第一段无 `AS` 别名时列名退化为 EXPR$N，外层 SELECT 引用失败。修复：只给第一段加别名（后续段继承）。当日 ads 分支首段漏别名（dws 分支首段有所以成功）。
5. 修复后**只提交第 4 段**（前 3 分支已在跑）：多 INSERT 文件重跑会重复提交所有分支 → 同 topic 同 group.id 重复消费 → 瓜分 bug（见"修复"节）。教训：多分支 SQL 文件改单个分支时，要么 cancel 全部重提（幂等），要么抽单段单独提。

### 如何验证（批流对账 5/6 指标）
批基准（生成器确定性重放 seed 20260901）：`2026-09-01 browse_pv=641, day_uv=365`

| 指标 | 流值（ads_traffic_day） | 批值 | 结果 |
|---|---|---|---|
| browse_pv | 641 | 641 | ✓ |
| day_uv | 365 | 365 | ✓ |

- dws_traffic_day topic：upsert 持续累计覆盖 ✓
- 分钟层不受影响：ads_traffic_1m SUM=636 ✓（= 641 − 5 尾部，预期差异）
- Flink：job1×3 + job2_dwd×4 + job2_trade×2 = 9 作业全部 RUNNING

### 出错重跑方法
```
# 全量重提（cancel 同名旧作业后）：
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
# 只换单个分支：抽该段（DDL+INSERT）成临时文件单独提交（见本节"为什么"5）
```

---

## 节点 5：批流对账（6 指标总表）✅ 2026-09-03

### 做了什么
汇总 6 指标做一次总对账：流侧查 gmall_report_rt（ads_traffic_day + ads_trade_day），批侧查 gmall_rt（payment_info / order_refund_info）+ 生成器确定性重放（流量源在 Kafka，无批表）。测试日 2026-09-01（seed 20260901，manifest_20260901.txt）。

### 对账总表（流 = 批 全 ✓）

| # | 指标 | 流值 | 批值（批 SQL/重放） | 结果 |
|---|---|---|---|---|
| 1 | browse_pv（日） | 641 | 641 | ✓ |
| 3 | day_uv | 365 | 365 | ✓ |
| 4 | gmv | 1,136,485.35 | 1,136,485.35 | ✓ |
| 5 | payment_order_count | 182 | 182 | ✓ |
| 6 | payment_user_count | 83 | 83 | ✓ |
| — | refund_amount（按日） | 9/1: 3,680.40 / 9/2: 27,535.37 / 9/3: 17,848.42 | 同 | ✓ |

（refund_amount 跨 9/1–9/3：生成器模拟退款滞后支付 1-2 天，聚合口径=按退款时间归日，流批逐日一致。）

### 差异说明（预期差异，非 bug——README I.7 定义）
1. **分钟 PV（636）≠ 日 browse_pv（641）**：差 5 条尾部 browse。根因：2000 条一次性回放，最后一个事件窗口未等 watermark 触发就无数据了 → 分钟窗口少 5；日指标从明细重算不受影响。**验证了"日指标独立重算"的必要性**（若日 PV 从分钟 SUM 上卷会丢 5）。
2. **分钟 UV 不参与日对账**：当前分钟表只产出 browse_pv；若产出分钟 UV 也不可 SUM 当日 UV（红线，同用户跨窗口）。

### 面试价值
- 每个指标都有**独立可复现的批基准**（MySQL 批 SQL + 生成器同 seed 重放），流侧 30s checkpoint 后任意时刻重查一致 → 论证流批一致性的方法本身可演示。
- 差异是**分类记录**的：真 bug（共享 group、重复计数）→ 修复并回归；预期差异（尾部窗口、分钟 vs 日不可加）→ 写清原因不消除。面试讲"你怎么知道流算对了"的答案在这里。

### 出错重跑方法
全部指标重跑 = 节点 2/3/4 的 SQL 重新提交（各自文件），批基准按 manifest + 重放命令重现；核对完成后重跑本表查询即可。

---

## 节点 6：拆三作业（Job3 无状态 sink）——物理四层成立 ✅ 2026-09-03

### 做了什么
1. [job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql) 精简：删 ads_traffic_1m/ads_traffic_day 两个直出 MySQL 的分支与专属 source → 只留 dws 计算（2 分支）
2. [job2_trade_dws.sql](../flink-sql/job2_trade_dws.sql) 精简：删 ads_trade_day 分支 → 只留 dws_trade_day（1 分支）
3. 新建 [job3_ads_sink.sql](../flink-sql/job3_ads_sink.sql)：3 分支无状态转发（dws_traffic_1m→ads_traffic_1m / dws_traffic_day→ads_traffic_day / dws_trade_day→ads_trade_day），每分支独立 group.id
4. 部署顺序（关键）：先 cancel 旧 job2 全 6 作业 → 提交 Job3（从 dws topic 存量接管写 MySQL，无空洞）→ 再提交精简 Job2（重算 dws topics，Job3 实时转发覆盖收敛）

### 为什么这么做（面试点，README 红线"拆作业前禁止写物理四层"）
1. **物理四层成立**：ODS(MySQL/Kafka) → DWD(CDC/去重) → DWS(聚合) → ADS(落库) 各一层专属作业，拓扑即简历上写的"四层实时数仓"。
2. **Job3 无状态论证（设计 V2 决策 6）**：读 DWS 聚合后的当前绝对值（upsert-kafka 同 key 已合并、无 changelog 计算）+ 同主键覆盖写 MySQL → 无窗口/去重/累计算子、无状态。好处：重启秒级、天然幂等（重放 N 次收敛同值）、可用多并行扩展吞吐。
3. **指标只算一次**：MVP 的 ads 直出分支从 DWD 明细重复计算（每个指标双份窗口/去重状态）；拆后 Job2 算一次写 dws topic，Job3 只转发 → 计算成本减半、口径天然一致（ads 与 dws 不再可能因两套计算差异打架）。
4. 三个 INSERT = 三个独立作业，group.id 必须独立（既有教训）。

### 如何验证
- 9 作业全部 RUNNING：job1×3（ODS→DWD）+ job2×3（DWD→DWS）+ job3×3（DWS→ADS）
- MySQL 与拆前逐值一致（拆作业不改数据是正确性验证）：
  - ads_traffic_1m：33 窗口、PV 汇总 636 ✓
  - ads_traffic_day：browse_pv 641 / day_uv 365 ✓
  - ads_trade_day：gmv 1,136,485.35 / 订单 182 / 用户 83 / refund 按日 3 行 ✓
- Job3 单作业 task 图应无聚合算子（纯 source→sink）

### 出错重跑方法
```
bash scripts/submit_sql.sh flink-sql/job1_ods_to_dwd.sql job1_ods_to_dwd
bash scripts/submit_sql.sh flink-sql/job2_dwd_to_dws.sql job2_dwd_to_dws
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws
bash scripts/submit_sql.sh flink-sql/job3_ads_sink.sql job3_ads_sink
```
（cancel 同名旧作业后按 Job1→Job2→Job3 顺序提交）

---

## 节点 7：Grafana 看板（provisioning 自动注册）✅ 2026-09-03

### 做了什么
1. 新建 [grafana/provisioning/](../grafana/provisioning/)（datasources/mysql.yaml：MySQL 数据源 uid=mysql-rt + dashboards/dashboards.yaml：file provider 10s 热更新）
2. 新建 [grafana/dashboards/realtime-overview.json](../grafana/dashboards/realtime-overview.json)：8 面板——GMV/支付订单/支付用户/退款金额/日 PV/日 UV 六个 stat + 分钟 PV 柱状时间序列 + ADS 明细表
3. [docker-compose.yml](../docker-compose.yml) grafana 挂载两目录（只读）+ depends_on mysql healthy
4. `compose up -d grafana` 重建 → API 验证数据源/看板注册 → 代理查询面板 SQL 确认能取数

### 为什么这么做（面试点）
1. **provisioning 而非 UI 手点**：数据源/看板是配置文件 → 随 compose 可复现（环境重建不丢配置），简历讲"Grafana 面板代码化管理"。
2. **uid 固定（mysql-rt / rt-realtime-overview）**：dashboard JSON 与数据源解耦引用，不依赖 Grafana 自生成 uuid。
3. **时区处理**：ADS 的 window_start 是 MySQL DATETIME（本地墙上时间，无时区语义），Grafana MySQL 插件按 UTC 解析 → 时间序列面板用 `CONVERT_TZ(window_start,'+08:00','+00:00')` 显式归一到 UTC 再让 Grafana 显示 Asia/Shanghai（否则曲线偏移 8 小时）。
4. **stat 面板取"最新日值"**（ORDER BY metric_date DESC LIMIT 1）：指标表按日 upsert，看板永远展示当前值；分钟序列用 $__timeFilter 走 Grafana 时间范围。

### 如何验证
- `GET /api/datasources` → mysql-rt（MySQL 实时数仓 → mysql:3306）✓
- `GET /api/search` → rt-realtime-overview（电商实时数仓 · 实时指标总览）✓
- `POST /api/ds/query` 代理查 GMV → 1136485.35、日 UV → 365 ✓（面板 SQL 数据链路通）
- 浏览器 http://localhost:3000（admin/admin）可见看板

### 出错重跑方法
```
docker compose up -d grafana   # provisioning 文件改动后重建（或等 10s 热更新看板）
```

---

---

## 节点 8：分钟指标补齐（流量 UV + 交易分钟）+ 两个排障 ✅ 2026-09-03

### 做了什么
1. **流量分钟 UV**（[job2_dwd_to_dws.sql](../flink-sql/job2_dwd_to_dws.sql)）：分钟 INSERT 改 UNION ALL 加 `browse_uv` 段（窗口内 COUNT DISTINCT uid）→ 验证 33 窗口：browse_uv 汇总 627 ≤ browse_pv 636 ✓（UV≤PV 语义成立）
2. **交易分钟表**：job2_trade_dws.sql 增加 `dws_trade_1m` sink + 分钟窗口 INSERT（gmv / payment_order_count 两指标 UNION ALL），source `dwd_payment_detail` 补 WATERMARK FOR create_time（-5s）；[job3_ads_sink.sql](../flink-sql/job3_ads_sink.sql) 加第 4 分支（dws_trade_1m→ads_trade_1m，JDBC 落 MySQL）
3. **排障 A —— 共享 consumer group**：日 INSERT 与分钟 INSERT 两个独立作业共用同一 source DDL（group job2_trade_dws_group）→ 1 partition 被 rebalance 瓜分，分钟作业消费中断在 12:04（43 窗口）。修复：分钟段独立 source DDL `dwd_payment_detail_1m` + 独立 group.id
4. **排障 B —— 事件时间乱序（真根因）**：换独立 group 后仍只 43 窗口 → 分层排查（见下）→ 发现 Kafka topic 内事件时间非单调 → 新建 [replay_dwd_payment_sorted.sh](../scripts/replay_dwd_payment_sorted.sh) 一次性按 create_time 排序重放 → job2_trade source 指向 `dwd_payment_detail_sorted`
5. **一键启动脚本** [start_env.sh](../scripts/start_env.sh)：compose up + 健康等待 + topic 数据层检查 + 按依赖顺序补齐作业（幂等可重跑）

### 为什么这么做（面试点）
1. **分钟粒度是趋势图基础**：日指标只有 1 个点画不了曲线；分钟 PV/UV/GMV 驱动 Grafana 时间序列（真实数仓的"实时"叙事 = 分钟级刷新）。
2. **排障 A 教训**：Flink SQL 文件内多个 INSERT = 多个独立作业，**每个作业消费的 source 都要独立 DDL + 独立 group.id**（job2_dwd 的日段早已独立成 dwd_traffic_event_day；trade 加分钟 INSERT 时漏了 → 同一坑踩第二次，证明"多作业共享 group"不是一次性失误而是结构性陷阱）。
3. **排障 B 教训（最值钱）**：分层排查法——先证数据透传（临时无窗口作业按小时 COUNT 全量 182 ✓，12:00+ 有 133 笔）排除 source/normalize → 再证窗口层（只 fire 43）→ dump topic 时间序列找乱序。机制：**upsert-kafka 的 watermark 按物理消费序推进，Kafka 内事件乱序 → 先读到晚事件水位跳高 → 其后到达的早窗口事件被判"迟到"丢弃**（watermark 只前进不后退）。
4. **乱序根因是"重灌伪影"不是系统缺陷**：测试数据 payment 的 id = 流水号，与支付时间错位；CDC snapshot 按主键扫描输出 → 非时间序。真实生产 Kafka 事件序 ≈ binlog 提交序 ≈ 发生序，不会有此问题。重排工具把数据重放成"真实事件流" = 与流量域 generator replay 同哲学（数据重放 = 确定性回放测试数据）。

### 如何验证（对账闭环）
- ads_trade_1m：**162 窗口**（08:21–23:27 全部有支付的分钟）× 2 指标
  - gmv 合计 1,119,295.10 = 日 GMV 1,136,485.35 − 尾部 23:32:16 单笔 17,190.25 ✓（分毫不差）
  - 订单 181 = 182 − 1 ✓；抽查 12:04/14:19/22:55 三个分钟窗口与 MySQL payment_info 同分钟聚合**逐值相等** ✓
- 尾部差异说明：[23:32,23:33) 单笔窗口不 fire（最后水位 23:32:11 < 窗口 end）——与流量尾部同机制，属预期（面试口径："测试日最后一条记录所在窗口因无后续事件推进水位而不触发"）
- 11 作业 RUNNING：job1×3 + job2_dwd×2 + job2_trade×2 + job3×4；日表 4 指标复查不变（gmv 1,136,485.35 / 182 / 83 / refund 49,064.19）

### 出错重跑方法
```
# 交易分钟重算（幂等：全新 group 从 earliest 全量重算，upsert 覆盖收敛）
bash scripts/replay_dwd_payment_sorted.sh     # 重排数据（可重跑，幂等：删 topic 重建再灌）
# cancel 旧 job2_trade_dws 后：
bash scripts/submit_sql.sh flink-sql/job2_trade_dws.sql job2_trade_dws

# 日常重启环境（幂等）：
bash scripts/start_env.sh
```

---

---

## 节点 9：Grafana 看板分钟层补齐（9 面板）✅ 2026-09-03

### 做了什么
1. [realtime-overview.json](../grafana/dashboards/realtime-overview.json) 升级 8 → 9 面板：
   - 分钟 PV 面板（id 7）升级为**双系列**：browse_pv + browse_uv（分钟 UV 上同一时间轴对比，半宽）
   - 新增**分钟成交额 GMV 趋势面板**（id 9，timeseries，12 列宽，人民币单位）：ads_trade_1m WHERE metric_name='gmv'
2. 排障：Grafana admin 密码失效（`GF_SECURITY_ADMIN_PASSWORD` 只在 admin 用户**不存在**时生效，用户已存在则不变）→ 容器无数据卷 + 全 provisioning 管理 → `docker compose up -d --force-recreate grafana` 强重建 = 干净初始态，看板/数据源零丢失自动恢复（provisioning 可复现性实证）

### 为什么这么做（面试点）
1. **节点 8 的分钟指标要"看得见"**：分钟 UV/GMV 是趋势图数据源，看板闭环（数据→指标→可视化）是简历 demo 的最后一公里——打开 Grafana 即演示分钟级刷新。
2. **PV/UV 同图**：直观展示 UV≤PV 语义（面试可指图讲 watermark 窗口与去重差异）；GMV 用折线 + 金额单位区分交易域（与流量柱状视觉分层）。
3. **provisioning 全声明式**（数据源 uid + 看板文件）：容器删了重建自动恢复 —— 简历可讲"配置即代码，Grafana 零手工配置"。

### 如何验证
- API 读看板：9 面板注册 ✓（id 7 targets=2、id 9 GMV 面板在）
- `POST /api/ds/query` 代理执行两条新 SQL：GMV 分钟序列返回 **162 行**（= ads_trade_1m 窗口数）✓；browse_uv 返回 **33 行**（= 流量窗口数）✓
- 浏览器 localhost:3000（admin/admin）：GMV 曲线 08:21-23:27 分钟点、PV/UV 双曲线

### 出错重跑方法
```
docker compose up -d --force-recreate grafana   # 凭据/状态异常时强重建（全 provisioning，无手工配置）
# 面板文件改动：等 10s 热更新即可（dashboard provider），无需重建
```

---

## 节点 10：下单侧链路（订单明细 CDC + 3 指标，加分 A）+ 学习交叉引用（加分 B）✅ 2026-09-03

### 做了什么
1. **Job1 加第 4 分支**：CDC(order_detail) → dwd_order_detail（[job1_ods_to_dwd.sql](../flink-sql/job1_ods_to_dwd.sql) 第 4 个 INSERT）。设计同支付/退款分支：主键 id（MySQL PK 保证唯一，透传无 ROW_NUMBER 去重）、sink PK=id；**server-id 5420-5424 独立段**（同作业多 CDC 源、跨作业都要分段，防 MySQL broker 踢下线）。
2. **Job2 交易日聚合 +3 指标**（[job2_trade_dws.sql](../flink-sql/job2_trade_dws.sql) day INSERT UNION 尾部追加 3 段）：
   - `order_gmv` = SUM(order_price × sku_num)（下单金额，按下单日归日）
   - `order_count` = COUNT(DISTINCT order_id)
   - `order_detail_count` = COUNT(*)
   - 下单侧消费**未经 sorted 重放的原始 dwd_order_detail**（对照支付必须 sorted：日聚合 GROUP BY DATE 不依赖水位 → 乱序天然无害）。
3. **start_env.sh 同步**：JOBS job1 3→4（幂等补齐逻辑直接支持 12 作业）。
4. **加分 B**：README 新增 §7「项目经验 ↔ 学习资料交叉引用」11 行对照表（锚点/bug × Flink 笔记讲次/SQL 错题册章/面试手册章）；三份笔记文件头加"反向索引（项目回链）"行，双向闭环。
5. 误双提交留下的 15 个互相踩 server-id 的残留作业清场 + start_env CRLF bug 修复（见下"排障"）。

### 为什么这么做（面试点）
1. **订单是交易生命周期的入口**：此前只有支付/退款两链路，下单→支付→退款只覆盖 2/3。补上订单明细后锚点形成**下单 GMV 1,334,596.00（200 单）→ 支付 GMV 1,136,485.35（182 单）**的漏斗对照 = 简历可直接讲的"交易完整生命周期 + 转化视角"，且死表顾虑解除（此前 DWD 无订单表、订单明细 398 行躺在源库 = 无消费者的死数据）。
2. **两套消费策略对照（面试题级）**：窗口链路（支付）必须事件时间单调（sorted 重放）；非窗口日聚合链路（订单）乱序无害。回答"什么时候需要处理乱序/水位"时，用"聚合按时间分桶则需要，按无限键累计则不需要"概括，正好两个工程实例都在这。
3. **下单侧不加分钟窗口**：与支付分钟不同，订单分钟趋势无业务需求（成交额曲线支付口径够用），避免无谓窗口状态 → 这是"不为炫技加状态"的口径克制，可讲。

### 排障（本节点两个坑，均入 README bug list #11）
- **坑 A：提交笔误双跑 → 作业风暴**。`submit | grep -cE "Job ID"` 后又整跑一次 submit → 同 server-id 段 CDC 作业 8 个并存 → MySQL broker 互踢下线 → 作业无限 RESTARTING，cancel 与 restart 赛跑：cancel 后 RESTARTING 作业按重启策略又拉起来 → 一度 15 个（4 RUNNING + 11 RESTARTING）且旧健康作业也陷在 cancel 竞赛里。**正确恢复 = `docker restart gmall_jobmanager`**（standalone 无 HA：作业注册在 JM 内存，重启全灭；Kafka/MySQL 数据在 named volume 不丢）→ start_env 按依赖序一键重提 12 作业 = 干净收敛。教训：作业风暴不要逐 job cancel（异步 cancel 赶不上 restart），用"全灭重建"；CDC server-id 段是作业间互踢的根因，任何一次双提交都会触发。
- **坑 B：start_env.sh CRLF 毒 URL**（修复 [start_env.sh](../scripts/start_env.sh)）：cancel_name 管道里 docker exec 输出经 **Windows docker.exe → MSYS 管道被 CRLF 化**（空输出变 `\r\n`），`read` 剥 `\n` 后残留裸 `\r` → `curl -X PATCH http://.../jobs/\r` 畸形 URL exit 3 → 脚本死。修复：JM 变量规范化成 `http://localhost:8081` + cancel 管道插 `tr -d '\r'`。教训：Git Bash 下凡 read 管道接 Windows 原生程序输出，都要防 `\r`。

### 如何验证（对账闭环）
- **流 = 批 7/7 指标全对**（ads_trade_day，2026-09-01 行）：
  - 旧 4 指标不变：gmv 1,136,485.35 / payment_order_count 182 / payment_user_count 83 / refund_amount 3,680.40（9/2: 27,535.37、9/3: 17,848.42 退款日行也在）✓
  - 新 3 指标：**order_gmv 1,334,596.00 / order_count 200 / order_detail_count 398** ✓（= 批 SQL 锚点）
  - 差值对照：1,334,596.00 − 1,136,485.35 = 下单未支付部分（200 单 vs 182 支付单），漏斗第一层闭环
- 分钟层完好（重提未破坏）：ads_trade_1m **324 行 = 162 窗口 × 2 指标**、GMV 和 1,119,295.10（= 节点 8 口径：日 GMV − 尾部单笔 17,190.25）✓；ads_traffic_1m 66 行（33 × 2）✓
- 拓扑：**12 作业 RUNNING**（job1×4 / job2_dwd×2 / job2_trade×2 / job3×4）；dwd_order_detail topic 快照 **398 条** = MySQL order_detail 398 行 ✓

### 出错重跑方法
```
# 作业风暴恢复（清场后再重提，全程幂等）：
docker restart gmall_jobmanager      # standalone 无 HA：作业全灭，数据不丢
bash scripts/start_env.sh            # 一键补齐 12 作业（job1×4/job2×4/job3×4，含补齐等待）

# 下单指标单独重算（例如 topic 数据被清）：
#   dwd_order_detail 由 job1 CDC 快照重灌（幂等）；job2_trade_dws cancel 后重提即从 earliest 全量重算
#   结果表若被 DROP：mysql/ads_result_rt_ddl.sql（在 gmall_report_rt 库，独立于 gmall_rt 源库）
```

---

## 待办（工程可靠性，简历可讲）
- [x] mysql/kafka 数据卷持久化（named volume），Docker 重启不丢数据（节点 2 已完成：mysql-data + kafka-data）
- [x] docker-compose 加 `restart: unless-stopped`（节点 2 已完成）
- [x] 环境一键启动脚本 `scripts/start_env.sh`（compose up + topic 检查 + 作业补齐，节点 8 已完成）
- [x] 分钟指标补齐：流量分钟 UV + 交易分钟 dws_trade_1m/ads_trade_1m（节点 8 已完成）
- [x] Grafana 看板分钟层补齐（节点 9：PV/UV 双系列 + 分钟 GMV 面板）
- [x] 可选加分：dwd_order_detail 订单明细 CDC + 下单 3 指标（节点 10：下单金额/单数/明细行数流批对账 ✓，含配套下游，无死表）
- [x] 可选加分：README §7 交叉引用表 + 三份学习笔记反向索引行（节点 10：学以致用双向闭环）
- [x] 开源发布准备：二进制依赖出库 + 一键重建脚本 + 历史瘦身（见下节）

---

## 附：开源发布准备（2026-09-10，仓库工程化）

### 做了什么
1. **敏感信息体检**：全库扫描结论——无真实个人信息。仅有的"密码"全部是单机 demo 的本地测试凭据（MySQL root `123456`、Grafana `admin/admin`）、生成器造的假数据（`demo_pwd` / `@example.com` / 假手机号）；无真实手机号/身份证/邮箱/密钥。
2. **二进制依赖出库**：`flink-lib/` 17 个 jar（~230MB）从版本库移除 + `.gitignore` 忽略，改由 [scripts/fetch_flink_lib.sh](../scripts/fetch_flink_lib.sh) 幂等重建（13 个从 `flink:1.19.1` 镜像 `docker cp` 提取——版本与镜像自动锁定；4 个外部 connector 从 Maven Central 下载）。`start_env.sh` 检测到 `flink-dist` 缺失时自动调用（新增 step 0/6）。
3. **误跟踪文件清理**：`data-generator/__pycache__/*.pyc` 出库（`.gitignore` 早有规则，只是先于规则被 `git add` 进来了）。
4. **历史瘦身**：`git filter-branch --index-filter 'git rm -r --cached --ignore-unmatch flink-lib' --prune-empty -- --all`（24 提交全部重写，提交信息/日期/顺序保留，哈希变化——本地无 remote 无影响）。
5. README 新增 **§9 快速开始**（clone 后三步：fetch → start_env → 开面板）+ §5.1/§10 同步。

### 为什么（面试点）
- **GitHub 单文件 100MB 硬限制对"推送的所有对象"生效，包括历史提交**——只把大文件从当前索引 `git rm --cached` 是**不够的**：只要那个 blob 还挂在任一历史 commit 上，push 就会被 pre-receive hook 整个拒绝。正确做法是重写历史把它从每个 commit 的 index 里摘掉。
- **二进制产物/依赖本不该进版本库**（clone 体积、diff 噪音、无法 code review）。但在"数据工程项目"里它又必须能被一键还原——所以配套 **fetch 脚本 + 环境启动脚本自动检测**，把"缺失即重建"做成闭环，而不是让使用者读文档手工下载。这与项目里 `replay_*.sh`（数据重放）是同一哲学：**环境与数据的可重现性都要脚本化**。
- 分层依据：13 个"发行版自带"的对齐镜像是为了**版本不出错**（手写 Maven 坐标最容易配错版本）；只有 4 个真正需要外挂的 connector 才走下载。

### 怎么验证
- 清理前：`git rev-list --objects HEAD | git cat-file --batch-check | awk '$3>50000000'` → 命中 120MB 的 `flink-dist-1.19.1.jar`。
- 清理后：同一命令对 `main` 输出为空；全历史最大文件 0.21MB（`business_data_20260901.sql`）；跟踪文件 48 → 31。
- 本地 jar 未受影响（重写只动 index，不动工作区）：`ls flink-lib/*.jar | wc -l` = 17 ✓。
- fetch 脚本幂等分支实测：`bash scripts/fetch_flink_lib.sh` → 全部"已存在，跳过"，exit=0 ✓。
- 安全网：`refs/original/refs/heads/main` 保留重写前哈希（29feb50 的原始版本），确认推送成功后可删。

### 出错重跑方法
```
# flink-lib 丢失/全新 clone：
bash scripts/fetch_flink_lib.sh        # 幂等；需 Docker daemon 可用（提取镜像 lib）

# 若历史重写后发现问题，回滚到重写前：
git reset --hard refs/original/refs/heads/main
git branch -f main refs/original/refs/heads/main

# 推送成功、确认无需回滚后，回收 .git 空间（当前 215MB，含旧历史副本）：
rm -rf .git/refs/original && git reflog expire --expire=now --all && git gc --prune=now
```

### 遗留
- fetch 脚本的**镜像提取分支**与 **Maven 下载分支**尚未端到端实测（Docker Desktop 当时未运行）——首次全新 clone 或删库重跑时可验证；URL 已用 HTTP 探测确认 200。
- 推送前建议：`git log --stat` 再目视一遍 / GitHub 建空仓库时选 Public、**不要**勾 "Add a README"（避免首推冲突）。

### 发布结果
- 已推送至 **https://github.com/X1n666/realtime-data-warehouse**（Public）：25 提交 / 31 跟踪文件，首推实测仅 **210 KiB**（历史重写生效——121MB 的 blob 若还在，这里会是 ~230MB 且被 GitHub 拒绝）。
- `flink-lib/` 未出现在远程（符合预期，由 `fetch_flink_lib.sh` 重建）。

### 排障：GitHub 认证 403（`Permission ... denied to X1n666`）
现象：`git push` 反复被拒，报 `Permission to X1n666/realtime-data-warehouse.git denied to X1n666`——**认证认出了账号，但凭据无权限/已失效**。
诊断法（全程不打印 token 明文）：
```bash
# 1) 取出当前生效的凭据(password 字段)，只喂给 curl，不落到屏幕上
printf "protocol=https\nhost=github.com\n\n" | GIT_TERMINAL_PROMPT=0 git credential fill
# 2) 用它打 GitHub API，看状态码即可定位：
#    401 = token 失效/无效；403 = token 有效但缺 scope 或无权访问该仓库；200 = 正常
curl -sI -H "Authorization: token $TOKEN" https://api.github.com/user | grep -iE "^HTTP/|x-oauth-scopes"
```
本例结论：GCM(Windows 凭据管理器)里存着一个**失效凭据**，每次 push 都被复用它 → 401/403。
修复 = `git credential reject` 清掉坏凭据 → 重新登录一次（浏览器 OAuth 或 classic PAT 勾 `repo` scope）。
坑中坑：全局配了 `credential.helper=store`，会把新凭据**明文**写进 `~/.git-credentials`（本例 8 月 18 日那条陈旧凭据就是这么来的）——应改用 GCM 托管。

---

## 附：面试加固（2026-09-16）

针对"面试官必问、当前答不上来"的几处做的补齐。产出：[docs/面试速览卡.md](面试速览卡.md)（面试前 10 分钟速览）。

### 做了什么
1. **job1 显式设置去重状态 TTL**：`SET 'table.exec.state.ttl' = '24h'`（原来完全没设，走的是默认值）。
2. **README 新增 §4.5「状态管理与容错」**：状态盘点表（哪些有界/无界）、TTL 取舍、端到端一致性边界。
3. **新增面试速览卡**：七步主线 + 每步一个可讲的坑 + 6 个深挖问答 + 诚实清单 + 数字速查。

### 为什么（关键事实，面试最容易答错的点）
- **开源 Flink 的 `table.exec.state.ttl` 默认值是 `0` = 永不过期**。字节码实证（1.19.1 `flink-table-api-java-uber` 里的 `ExecutionConfigOptions`，字段名是 `IDLE_STATE_RETENTION` 而非 `TABLE_EXEC_STATE_TTL`，这也是第一次按字段名搜不到的原因）：
  ```java
  ConfigOptions.key("table.exec.state.ttl").durationType()
      .defaultValue(Duration.ofMillis(0))          // ← lconst_0
      .withDescription("... Default value is 0, which means that it will never clean up state.");
  ```
  **阿里云 VVR ≥ 4.0.12 默认 1.5 天，与开源版不同**——网上大量资料把两者混为一谈，答错就是硬伤。
- **不设 TTL 的后果**：job1 的 `event_id` 去重状态 ∝ 累计去重键数线性增长 → checkpoint 变大、恢复变慢、最终 OOM。
- **为什么只给 job1 设、job2 刻意不设**：
  - job2 的**窗口状态**在窗口 fire 后自动清理，不需要 TTL；
  - job2 的**日聚合状态**按 key 基数有界（日期 × 指标，规模极小），但它的 `COUNT(DISTINCT uid)` 保留去重集合——**若 TTL 过短，迟到数据到达时状态已被清理，聚合会从零重算写出错的日指标**（可能报 `Can not retract a non-existent record`）。**结论：TTL 不是越短越好，取值必须小于业务可接受的重算窗口。**

### 怎么验证（改的是 SQL，需重跑作业才生效）
```bash
# 1) 提交改动后的 job1（会重新读 SET 语句）
docker restart gmall_jobmanager && bash scripts/start_env.sh
# 2) 确认作业起得来、指标不变（TTL=24h 远大于数据时间跨度，不该影响任何结果）
#    日口径 9 项锚点应仍是 §4.3 的值，分钟层 33/162 窗口不变
# 3) 想直观看到 TTL 生效：调小 TTL 复现状态清理（如 '5min'），观察 job1 状态大小
#    变化与 checkpoint 体积——注意这会让重复 event_id 在 TTL 后被当作新事件放行
```

### 仍未做（面试如实说"还没做"比含糊带过安全）
- **checkpoint 故障恢复演练**：`docker kill gmall_taskmanager` → 观察自动恢复 + 状态不丢 + 指标不重复。**这是最该补的一项**，需要环境运行时做。
- **维度建模**：`user_info` / `base_province` / `base_category` / `spu_info` / `sku_info` 五张维表**被 4 个 SQL 文件引用 0 次**——项目目前只有事实表、没有维度，缺 Lookup Join（分省 GMV / 品类 TopN）。
- 监控（Prometheus + 反压 + Kafka lag）、并行度实验（当前全为 1）、Savepoint 演练。

---

## 附：checkpoint 存储位置排查（2026-09-16）

**背景**：TTL 改动重跑后去查 checkpoint 是否正常，顺手确认"容错到底能容到哪一层"，发现一个此前从没验证过的**真实局限**。

### 怎么查的（都是只读命令，不影响运行中的作业）

```bash
# 1) 集群级有效配置里有没有 state.checkpoints.dir
curl -s http://localhost:8081/jobmanager/config        # → 20 条，无任何 state.* 键
grep -rn "state\.checkpoints\|checkpoints\.dir" .      # → 仓库内 0 命中（只有 num-retained）

# 2) 运行中作业的运行时真值（比翻文档可靠）
curl -s http://localhost:8081/jobs/<jid>/checkpoints/config
# → state_backend=HashMapStateBackend
#    checkpoint_storage=JobManagerCheckpointStorage
#    externalization={"enabled":false,"delete_on_cancellation":true}

# 3) 实际 checkpoint 体积与耗时
curl -s http://localhost:8081/jobs/<jid>/checkpoints
# → job1 4.3KB/4ms  job2_dwd 0.60MB/7ms  job2_trade 0.18MB/12ms  job3 0.05MB/3ms  failed=0
```

### 结论（三条，都能落到字节码/接口证据）

1. **checkpoint 全在 JobManager 堆内存**（`JobManagerCheckpointStorage`），而 jobmanager 容器**只挂了 `./flink-lib`，没有任何持久化卷** → **JM 丢失 = checkpoint 全丢 = 无从 restore**。叠加 standalone **无 HA**（JM 重启后作业不会自动拉起），所以本项目的恢复路径实际是**无状态全量重算 + 幂等覆盖写**，不是 restore。这条已在重提交演练里实测过（9 项指标与基线逐值一致）。
2. `externalization.enabled=false` → cancel 时 checkpoint 一并删除，也保不了。
3. **`JobManagerCheckpointStorage` 有 5 MB 硬上限**：`CheckpointStorageLoader` 在 `state.checkpoint-storage` 与 `state.checkpoints.dir` 都未设时会 WARN 并回落到默认类型；该类型用 `MemCheckpointStreamFactory` 落状态，`checkSize()` 超过 `DEFAULT_MAX_STATE_SIZE = 5242880`（字节码实证）时**抛 IOException 直接失败**，不是告警。异常文案：`Size of the state is larger than the maximum permitted memory-backed state. Size=%d, maxSize=%d. Consider using a different checkpoint storage, like the %s.`
   → 当前 0.6 MB 很安全，但**去重状态增长（比如把 `table.exec.state.ttl` 调大）会撞这个崖**——这条和 job1 的 TTL 决策是同一个话题的两面。

### 修复方向（**未实施**：需重建容器，会终止当前 12 个作业）

```yaml
# docker-compose.yml
jobmanager:
  environment:
    FLINK_PROPERTIES: |
      state.checkpoints.dir: file:///opt/flink/checkpoints
  volumes:
    - ./flink-lib:/opt/flink/lib
    - flink-checkpoints:/opt/flink/checkpoints     # 与 TM 同卷同路径
taskmanager:
  volumes:
    - ./flink-lib:/opt/flink/lib
    - flink-checkpoints:/opt/flink/checkpoints
volumes:
  flink-checkpoints:
```

设了 `state.checkpoints.dir` 后存储会切到 `FileSystemCheckpointStorage`；`file://` 路径要求 **JM 与 TM 两容器同路径可达**，所以必须挂同一个命名卷。配套可选 `execution.checkpointing.externalized-checkpoint-retention: RETAIN_ON_CANCELLATION`。

### 仍未做
- 上面的 compose 修复（待定：要重建容器 → 作业全灭 → 需重新提交）。
- **checkpoint 恢复演练**（`docker kill gmall_taskmanager`）——用户明确要求**先别做**，需其在场；且当前存储配置下"从 checkpoint restore"这条路本身走不通，演练前应先做上面的修复，否则只能验证到"作业重新拉起 + 全量重算"。

---

## 附：维度建模（Lookup Join）——已写好并验证，待上线（2026-09-16）

**背景**：项目一直只有事实表没有维度——`user_info` / `base_province` / `base_category` / `spu_info` / `sku_info` 五张维表被 4 个 SQL 文件引用 **0 次**。补维度是面试最容易被追问的缺口，趁 Docker 在跑把它做实。

### 第一步：先做数据侧侦察（决定方案可不可行）

```bash
docker exec gmall_mysql mysql -uroot -p123456 -B -e "..."   # 见 sql/recon_dim_lookup.sql 对账 0
```

结果——**五张维表都齐、关联全命中，方案可行**：

| 关联                          | 命中 / 总数 | 说明                              |
| ----------------------------- | ----------- | --------------------------------- |
| `order_info.province_id → base_province.id` | **200 / 200** | 无 NULL，12 个行省，全命中        |
| `order_detail.sku_id → sku_info.id`         | **398 / 398** | 全命中                            |
| `sku_info.category_id → base_category.id`   | **11 / 11**   | 全命中                            |
| `payment_info.order_id → order_info.id`     | **182 / 182** | 全命中                            |

**同时发现一个关键事实**：DWD 层**没有 order_info 流**（job1 只接了 payment / refund / order_detail 三条 CDC），而 `province_id` 只存在于 MySQL 的 `order_info` 里 → 分省指标**必须**用 Lookup Join 补这一跳，不是"想用"，是"只能这么用"。

### 第二步：用 EXPLAIN 试错（**没提交任何作业、没占 slot、不影响运行中的 12 个作业**）

两个探针，连撞两堵墙——**这两条约束文档里不显眼，但决定方案能不能落地**：

| 写法                                                | 报错                                                                                                                         |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `FOR SYSTEM_TIME AS OF p.create_time`（事件时间列）  | `Temporal table join currently only supports 'FOR SYSTEM_TIME AS OF' left table's time attribute field`                       |
| 给左表加 `WATERMARK FOR create_time` 后重试          | `Event-Time Temporal Table Join requires both primary key and row time attribute in versioned table, but no row time attribute can be found` |

**结论**：**JDBC `LookupTableSource` 不是"版本表"→ 事件时间 temporal join 根本用不了，只能配处理时间（`PROCTIME()`）**。想要事件时间版本语义，维表本身得是带 rowtime 的 changelog 流（CDC 维表 / Hive 分区维表）——那是另一套建模。

**意外收获**：`PROCTIME` **不需要水位**。这让作业可以安全消费 `dwd_order_detail` 这种**事件时间非单调**的 topic（就是节点8 那个坑的根源），天然绕开乱序问题。而且更本质的理由是——**JDBC lookup 本来就是"处理时刻点查"，没有按事件时间取历史版本的能力，硬套事件时间是假的精确**。

改 PROCTIME 后 **链式 lookup 编译通过**（`payment → order_info → base_province` 两层嵌套 LookupJoin），计划里聚合是 `SUM_RETRACT` → 必须 upsert-kafka 承载（节点2 老教训）。

### 第三步：产出（`flink-sql/job4_dim_lookup.sql`，**未提交**）

两条指标写进 `dws_trade_day`（与 job2 同表同主键）：
- `gmv_province` = 分省支付 GMV，`dimension_key` = 省份名
- `order_amount_category` = 分品类下单金额，`dimension_key` = 品类名

**job3 零改动**：它的 ADS 落库是**按 `(metric_date, metric_name, dimension_key)` 主键的纯转发**、不感知 `metric_name` 取值 → job4 新增的指标会被自动转发进 MySQL `ads_trade_day`，Grafana 直接可查。这是长表设计的红利：**加维度指标不动下游**。

### 第四步：批侧对账锚点（`sql/recon_dim_lookup.sql`，**已跑通**）

维度指标最漂亮的地方在于——**它自带完整性校验**：

| 断言                                          | 批侧实测        | 结论 |
| --------------------------------------------- | --------------- | ---- |
| `SUM(gmv_province)` = 日锚点 `gmv`            | **1,136,485.35** | ✓    |
| `SUM(order_amount_category)` = 日锚点 `order_gmv` | **1,334,596.00** | ✓    |

**为什么这条校验比看上去重要**：维度求和回到日锚点，等价于证明了 **join 的基数正确**——LEFT 丢行（维表未命中）会让和偏小，维表一对多导致行膨胀会让和偏大。两侧都严丝合缝 = 关联既不丢行也不重复行。**这是"加维度不改口径"的证明，不是巧合。**

明细（12 省 / 9 品类）见 `sql/recon_dim_lookup.sql` 实际输出，例如上海 160,175.45（17 单）居首、笔记本电脑 867,159.00（68 明细行）居首。

### 卡在哪：slot 不够（**需决策**）

`taskmanager` 的 `TASK_MANAGER_NUMBER_OF_TASK_SLOTS = 12`，当前 **12 个作业正好占满**；job4 两条 INSERT = 2 个新作业 → **需要 ≥14 个 slot**。而 `TASK_MANAGER_NUMBER_OF_TASK_SLOTS` 是容器环境变量，**改它必须重建 taskmanager 容器 → 正在跑的 12 个作业全灭**（standalone 无 HA，不会自动恢复）→ 需重新走 `start_env.sh` 全量重算。所以这一步和 checkpoint 修复一样，属于**需要用户点头的破坏性变更**。

（另一个可选方案：把两条 INSERT 用 `UNION ALL` 合成 1 个作业，只要 13 个 slot——省一个 slot，但 upsert sink 吃 UNION ALL 的 retract 流有额外风险，未验证。）

### 仍未做
- 上面的 slot 调整与 job4 提交（**待用户授权**）。
- 分省指标目前只有**支付口径**；若要做**分省下单金额**，得给 job1 加 order_info CDC 分支，或在 job4 里再 lookup 一次 `order_info` 取 `total_amount`（后者零 CDC 改动，更省）。

---

## 附：job4 上线 + 空 topic 静默故障排查（2026-09-16，14 作业）

### 一、授权后的合并变更（一次重建解决两件事）

用户授权后做了一次合并重建：① `state.checkpoints.dir` + 持久化卷（checkpoint 落盘）② slot 12→14（给 job4 腾位）。12 个旧作业全部终止 → `start_env.sh` 重提 → **14 作业 RUNNING**。

**checkpoint 落盘的验证**（不只看配置，看真有文件）：
```
GET /jobs/<jid>/checkpoints/config → checkpoint_storage = FileSystemCheckpointStorage
卷内实际文件: chk-1/_metadata, chk-2/_metadata（496K）
```
从 `JobManagerCheckpointStorage`（JM 堆）切到 `FileSystemCheckpointStorage`（落盘）成功——**Q1b 那个"checkpoint 存哪儿"的答案现在要改写了**。

**一个 Docker 坑（记下来，踩过一次）**：Docker 给"镜像里不存在的路径"创建挂载点时属主是 `root:root/755`，而 flink 镜像的 `/docker-entrypoint.sh` 会把 PID 1 降权到 `flink` 用户 → JM 建 checkpoint 目录时 EACCES，作业直接 `JobInitializationException: Failed to create directory for shared state`。
**假阳性陷阱**：`docker exec` 进去试 `mkdir` 会成功（exec 默认 root，绕过 entrypoint），很容易误判成"权限没问题"。**必须用 `ps` 看 PID 1 的真实用户**。
修法：加一个一次性 init 容器 `chown -R flink:flink`，且**必须覆盖 entrypoint**（否则 chown 也以 flink 身份执行 → `Operation not permitted`）。

### 二、重算验证的第一个坑：**我差点把"读旧值"当成"重算"**

重建后我核对 9 项日锚点，全部一致，一度准备写"全量重算幂等性通过"。但查顶点指标时发现 **`dwd_payment_dim` 读入 0 条** → 顺着查消费组 → **`job4_dim_province_group` 根本不存在**，而 `job4_dim_category_group` 有提交位点 1592/1592。

**为什么这很危险**：`dws_trade_day` 是 upsert-kafka，旧值一直躺在 Kafka 里；如果支付链路根本没重算，job3 会把**上一次运行留下的旧值**原样转发进 MySQL → 锚点当然"全都对得上"。**锚点一致 ≠ 这次算出来的**。

**教训（面试可直接讲）**：验证重算不能只看结果值，要看**数据流是否真的流过**——顶点指标/消费组位点是"过程证据"。更严的做法是先清空下游 topic 再重算，让"库里有的值"必然是本次产出（**clean-room 验证**）。

### 三、定位过程：三次假设被证据依次推翻

这是本次最有价值的部分——**每个假设都被实测推翻，最后才落到真因**。

| # | 假设 | 证据 | 结论 |
| - | ---- | ---- | ---- |
| 1 | 默认 `scan.startup.mode` 是 `group-offsets`，无提交位点时回退 LATEST | 字节码：`getStartupOptions()` 里确有 `orElse(StartupMode.GROUP_OFFSETS)`；`ReaderHandledOffsetsInitializer` 收 `startingOffset=-3`（NO_OFFSET 哨兵），把决策交给消费者 | **部分对**：默认模式确是 group-offsets，但"回退 LATEST"是错的 |
| 2 | 所以给 job4 两个 source 加 `earliest-offset` 就能修 | TM 日志：**`auto.offset.reset = earliest`**（不是 none/latest），消费者明确 `Seeking to earliest offset` | **推翻**：重置策略本来就是 earliest，改启动模式没用 |
| 3 | 那 topic 里有数据、只是没读到 | `kafka-get-offsets.sh --time -2/-1`：`dwd_payment_detail_sorted` **earliest=182 latest=182**（空 topic）；`dwd_order_detail` 0→1592（完好） | **推翻**：topic 是空的，读 0 条**是正确行为** |

真因在 **Kafka broker 日志**里，不在 Flink 侧：
```
Sent auto-creation request for Set(dwd_payment_detail_sorted) to the active controller.
Log for partition ... is renamed to ...-delete and is scheduled for deletion
Created log for partition ... in /tmp/kafka-logs/dwd_payment_detail_sorted-0 with properties {}
（60 秒后）Deleting segments as the log has been deleted
```

### 四、真因：三层叠加的**静默空读**

```
① auto.create.topics.enable=true（Kafka 默认）
   + 订阅该 topic 的消费者在线 → 任何 metadata 请求都会把刚删掉的 topic
     **自动重建为空 topic**（这就是 broker 日志里那句 auto-creation request）
② topic 删除是**异步**的，实测本机耗时 **整 60 秒**
   → 删除/重建/生产三者互相赛跑
③ 消费端 committed offset 落在新 topic 的合法区间内（正好 = 末尾）→ 不回退
```
结果：**topic 存在但为空 → 下游以 earliest 起读落在 log-start = 末尾 → 读 0 条**。
**最毒的地方是它完全静默**：作业保持 `RUNNING`、没有异常、指标是 0、消费组因为从未提交位点而"不存在"——只看作业列表会认为一切健康。

**复现实验（把归因钉死）**：消费者在线时，删除永远完不成——实测是 **60 秒一轮的删除/重建循环**；`cancel` 掉订阅它的 4 个作业后，**删除 1 秒完成**。这直接证明了"消费者自动重建"是那个变量。

### 五、连带发现：`kafka-data` 卷**挂错了路径**（持久化根本没生效）

排查中 `dir=/tmp/kafka-logs` 这行露了马脚：
```
docker exec gmall_kafka du -sh /tmp/kafka-logs /tmp/kraft-combined-logs
  → 14.0M   /tmp/kafka-logs        ← 真实数据（所有 topic）
  → 4.0K    /tmp/kraft-combined-logs ← compose 的卷挂在这里，是空的
```
**Kafka 实际写 `/tmp/kafka-logs`，而 `kafka-data` 卷挂在 `/tmp/kraft-combined-logs`** → 卷没用上，**所有 topic 数据都在容器可写层**：任何 `recreate`/`down`/换镜像都会静默清空全部 topic。
这和 MySQL 那次（compose 注释里"2026-08-24 两次丢失教训"）是**同一类问题在 Kafka 上重演**，但更隐蔽——因为 auto-create 会把"数据没了"伪装成"topic 正常、只是读不到"。
**暂未修复**（修复要重建 Kafka 容器，需先 `docker cp` 迁移数据或接受重放重建），已在 compose 里标注。

### 六、修好的两个工具 bug（`scripts/replay_dwd_payment_sorted.sh`）

| bug | 原实现 | 后果 | 修法 |
| --- | ------ | ---- | ---- |
| 删除没等生效 | `--delete` 后 `sleep 3` | 实测删除耗时 60s → create 撞 "already exists" → produce 写进一个**随后才被删掉**的 topic → **数据静默丢失**（就是本次空 topic 的来源） | 轮询 `--list` 直到 topic 真的消失（最多 120s），没落地就**直接失败退出** |
| 取了最旧副本 | `--max-messages 182` | 源 topic 是 upsert-kafka，job1 每次提交都全量重发 → 实测 **910 条 = 5×182**，取最老的 182 条 = 按"最旧版本"重组状态 | drain 全量 → **按 key 取最后一条**（真 upsert 语义）→ 再排序；并断言唯一 key 数 = 182 |

修复后一次跑通：`原始 910 条 → 去重后 182 个 key，时间单调性校验通过 08:21:26 .. 23:32:16` → `earliest=0 / latest=182 ✓`。

### 七、job4 上线验收（**两条维度锚点已在服务层对齐**）

| 指标                  | MySQL `ads_trade_day` 实测 | 锚点              |
| --------------------- | -------------------------- | ----------------- |
| `gmv_province`        | **12 行 / 1,136,485.35**   | = 日 `gmv` ✓      |
| `order_amount_category` | **9 行 / 1,334,596.00**  | = 日 `order_gmv` ✓ |

省份明细：上海 160,175.45 / 湖南 157,918.58 / 天津 114,806.63 / 江苏 109,417.59 / 河北 109,044.20 / 北京 83,260.12 / 四川 82,112.44 / 广东 81,886.42 / 浙江 77,690.70 / 湖北 69,929.95 / 福建 64,368.27 / 重庆 25,875.00。

**本轮是真重算**（不是上一次的"读旧值"）：`cancel` 后 `dwd_payment_detail_sorted` 的三个消费组被 Kafka 自动删除（空组无提交位点）→ 重提后**全新消费组 + `auto.offset.reset=earliest` → 从 0 重读 182 条**。省份分支 source 实测读入 **182** 条 = topic 全量。全量锚点复核：日口径 11 项（含 2 条维度）全部一致；分钟层 `traffic_1m` 33+33、`trade_1m` 162+162 窗口与金额均未回归。

### 八、给 `start_env.sh` 加了「空 topic 哨兵」

这类故障**只能靠显式断言发现**，所以把它写进了一键启动脚本（第 4.5 步）：对全部运行时 topic 检查 `earliest == latest`，命中就告警并给出修复指令。
**放在作业提交之后**检查——冷启动时 `dws_*` 本来就是空的（要等作业跑出来），放前面会误伤 `--full` 全新环境。

### 九、方法学教训（两条，都能在面试里讲）

1. **别用聚合指标代替日志**：我第一版结论"job1 没读、全链路没重算"就是错的——读 `/jobs/:jid` 的 source `write-records` 得到 0，而 TM 日志里 CDC 快照明明写着 `Finished exporting 182 records for split 'gmall_rt.payment_info:0'`（还有 398 / 12 条）。**顺序是：先看日志确认事实，再用指标做监控**。
2. **"存在但为空"是比"不存在"更危险的状态**：不存在会报错，存在但为空会被 earliest 静默吞掉。凡是"缺失 ⇒ 静默降级"的系统，都要显式断言非空。

### 仍未做 / 待决策
- **Kafka `kafka-data` 卷挂错路径的修复**（需重建 Kafka 容器；建议先 `docker cp` 迁移 `/tmp/kafka-logs` 再重建，并显式建 topic 而非依赖 auto-create）——**待用户决策**。
- `dwd_payment_detail_sorted` 的重建目前依赖**手工**跑重排脚本；`start_env.sh --full` 只提示不自动做。
- 分省指标目前只有支付口径（分省下单金额需再 lookup 一次 `order_info.total_amount`，零 CDC 改动）。
